# 淘到宝引擎 V0.5 后端与 AI Harness 需求文档

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 淘到宝引擎 |
| 文档版本 | V0.1 |
| 文档日期 | 2026-08-06 |
| 当前阶段 | MVP 设计与接口冻结 |
| 负责人 | Dev A（网关、存储、飞书接入与系统编排） |
| 协作方 | Molly（AI Schema、提取、检索、生成与质检） |

## 1. 文档目的

本文档用于明确淘到宝引擎 V0.5 中 Dev A 负责的服务端范围，并冻结服务端、前端与 AI 模块之间的接口边界。本文同时给出 AI Harness 的最小设计，使资料导入、资产提取、人工审核、检索和方案生成能够在同一条可观测、可重试、可追溯的链路中运行。

本文不替代完整产品需求，而是将现有 MVP 需求转化为可执行的后端和 AI 编排需求。

## 2. 当前进度

截至 2026-08-06：

### 2.1 已完成

- 已完成 V0.5 产品范围、P0/P1 边界和验收标准梳理。
- 已明确两人目录与职责边界。
- 已建立本地 Git 仓库和 `main`、`dev`、`feat/gateway` 分支。
- 已关联 GitHub 仓库 `wxh042/xianjintuan-engine`。
- 已形成本文档中的第一版接口清单与 AI Harness 契约。

### 2.2 正在进行

- 冻结 OpenAPI 字段、状态枚举和统一错误码。
- 冻结服务端调用 AI 模块的 Python 接口及结构化 Schema。
- 设计数据库九类核心对象和黄金流程。

### 2.3 尚未完成

- FastAPI 项目脚手架和业务接口尚未实现。
- PostgreSQL 表结构、迁移和 CRUD 尚未实现。
- 飞书 OAuth、飞书文档读取及 Mock 适配器尚未实现。
- AI Harness、后台任务和黄金流程自动化测试尚未实现。
- 前后端尚未开始真实接口联调。

## 3. 产品目标

淘到宝引擎面向神州数码售前及数字化转型团队，将分散在飞书中的客户资料、历史方案和产品 PRD 转化为可审核、可检索、可追溯的企业资产，并根据客户当前问题快速生成可继续追问的初步方案。

V0.5 需要验证以下闭环：

```text
登录
  -> 导入飞书资料或粘贴文本
  -> 提取客户画像、经验资产或原子能力
  -> 人工修改并审核
  -> 创建客户会话并输入当前需求
  -> 检索已审核经验和能力
  -> 生成有来源、有边界的八区块方案
  -> 保存结果并支持多轮追问
```

核心成功标准不是使用复杂算法，而是让客户直观看到：系统理解其问题、组合企业已有能力、说明依据和限制，并且在资料不足时不编造。

## 4. MVP 范围

### 4.1 P0 必须实现

- 飞书授权登录；演示阶段提供同 Schema 的 Mock 登录。
- 飞书文档链接导入，以及普通文本、会议转写和聊天记录粘贴导入。
- 资料处理任务、状态轮询、失败原因和手动重试。
- 一个客户一份共享画像，支持生成、修改和确认。
- 历史方案提取一条主要经验，支持修改、通过和打回。
- PRD 拆分多条原子能力，支持逐条修改、通过和打回。
- 一个客户多个独立会话，多轮消息保存与恢复。
- 经验和能力分库检索，只使用当前工作空间内已审核且来源有效的资产。
- 固定八区块方案、来源引用、事实边界标识和建议追问。
- 工作空间隔离、统一错误码、操作日志和健康检查。

### 4.2 P1 延后实现

- 邀请码、文档选择器、妙记接口直读和批量导入。
- SSE 流式输出和自动会话标题。
- 自动写入飞书文档和 Markdown 格式化接口。
- 字段级资料刷新差异摘要。

### 4.3 明确不做

- Multi-Agent、Deep Research、知识图谱和 GraphRAG。
- ChromaDB、pgvector、复杂混合检索和重排模型。
- Kafka、Kubernetes、微服务和复杂任务基础设施。
- 自动建群、专家协作、PPT 自动生成和飞书机器人。
- 企业全量资料同步、附件通用解析和实时变更订阅。

## 5. 人员与代码边界

### 5.1 Dev A 负责

- `app/api/`：FastAPI 路由、请求响应、鉴权和异常转换。
- `app/core/`：配置、错误码、日志、任务和 AI Harness 编排。
- `app/db/`：SQLAlchemy 模型、迁移、Repository 和事务。
- `app/integrations/`：飞书、模型及外部服务适配器。
- `openapi.yaml`：前后端唯一 HTTP 契约。
- 黄金流程接口测试和部署运行说明。

### 5.2 Molly 负责

- `app/ai/schemas/`：画像、经验、能力、检索结果和方案 Schema。
- `app/ai/prompts/`：信息提取和方案生成 Prompt。
- `app/ai/pipelines/`：画像提取、资产提取和方案生成算子。
- `app/ai/retrieval/`：候选资产排序、匹配原因和缺口说明。
- `app/ai/validators/`：引用校验、边界分类和防编造检查。

### 5.3 共享契约

- `app/contracts/` 和 `openapi.yaml` 属于共享边界。
- 修改字段、枚举、函数签名或错误码前必须双方确认。
- AI 模块不得直接操作 HTTP Cookie、飞书令牌或数据库事务。
- API 模块不得依赖 Molly 的 Prompt 和模型供应商实现细节。

## 6. AI Harness 定义

AI Harness 是连接业务服务与 AI 算子的运行编排层。它不负责具体 Prompt，而负责让每次 AI 调用具备统一输入、结构化输出、超时控制、重试、校验、日志、降级和溯源能力。

### 6.1 核心职责

1. 根据业务事件选择正确 AI 算子。
2. 组装最小必要上下文，避免将整库资料传入模型。
3. 为每次调用生成 `trace_id`、记录阶段、耗时和重试次数。
4. 对模型输出执行 Pydantic/JSON Schema 校验。
5. 对历史事实、企业能力和 `source_id` 执行引用白名单校验。
6. 将超时、限流、格式错误转换为统一业务错误。
7. 支持 `mock` 与 `live` 两种模式，二者输出结构完全一致。
8. 保存检索快照和生成结果，保证历史方案不会随资产变化而改变。

### 6.2 Harness 不负责

- 不直接实现飞书登录和文档抓取。
- 不直接决定资产审核是否通过。
- 不绕过 Repository 直接读写数据库。
- 不在 V0.5 内编排多个 Agent。
- 不把未经审核的资产作为历史事实或企业能力。

### 6.3 运行结构

```text
API Route
  -> Application Service
    -> AI Harness
      -> Context Builder
      -> AI Interface / Model Adapter
      -> Schema Validator
      -> Citation & Boundary Validator
    -> Repository / Transaction
  -> Job Status / API Response
```

### 6.4 资料处理链路

```text
pending
  -> fetching（飞书来源才需要）
  -> parsing
  -> extracting
  -> pending_review
  -> completed

任一可恢复阶段失败 -> failed -> 用户重试
```

处理规则：

- `customer_profile` 用途调用画像提取算子并形成画像草稿。
- `experience` 用途默认调用一次经验提取，形成一条主要经验草稿。
- `capability` 用途调用能力拆分，允许返回多条能力草稿。
- 提取结果只有通过 Schema 校验后才可入库。
- 草稿必须经过人工确认或审核后才能用于后续检索。

### 6.5 快速方案链路

1. 保存用户消息，创建 `solution_run` 和后台任务。
2. 读取客户最新画像和当前会话历史，不读取其他客户会话。
3. AI 根据精简的会话上下文提取结构化 `SearchIntent`，不得接收资产库数据。
4. Dev A 在数据库层根据 `SearchIntent` 分别检索经验和原子能力，排除硬约束冲突、软删除、未审核和来源失效的资产。
5. 数据库层按业务优先级排序并截断为最多 3 条经验和 5 条能力，固化画像、会话上下文和检索快照。
6. 只将精简后的检索快照传给 AI，调用方案生成算子。
7. 校验输出结构及所有引用标识。
8. 无效引用降级为 `ai_inference`，不得显示为企业事实。
9. 保存结构化结果和展示文本，更新运行状态。
10. 前端通过轮询取得结果并继续追问。

## 7. AI Python 接口契约

接口使用稳定业务对象，不传递 ORM 实例。第一版建议如下：

```python
from typing import Protocol

from app.ai.schemas import (
    CapabilityDraft,
    CustomerProfileDraft,
    ExperienceDraft,
    RetrievalSnapshot,
    SearchIntent,
    Solution,
    SolutionContext,
)


class AIEngine(Protocol):
    async def extract_profile(
        self,
        raw_text: str,
        source_ids: list[str],
    ) -> CustomerProfileDraft: ...

    async def extract_experience(
        self,
        raw_text: str,
        source_id: str,
    ) -> ExperienceDraft: ...

    async def extract_capabilities(
        self,
        raw_text: str,
        source_id: str,
    ) -> list[CapabilityDraft]: ...

    async def extract_search_intent(
        self,
        context: SolutionContext,
    ) -> SearchIntent: ...

    async def generate_solution(
        self,
        context: SolutionContext,
        retrieval_snapshot: RetrievalSnapshot,
    ) -> Solution: ...
```

约束：

- 所有接口均为异步，以便统一设置超时。
- `extract_search_intent` 只接收画像与会话上下文，输出关键词、行业、场景、问题、硬约束等结构化条件；不得接收或遍历资产库。
- 真正的检索、工作空间隔离、审核状态过滤、硬约束过滤、排序与 Top-K 截断均由 Dev A 在数据库层完成。
- `RetrievalSnapshot` 必须分别保存经验和能力；候选经验不超过 3 条，候选能力不超过 5 条。
- `generate_solution` 只能把 `retrieval_snapshot` 中的资产写为历史事实或企业能力。
- Molly 的 AI 算子遇到外部 LLM 超时、限流、网络错误或无效响应时必须直接抛出标准 Python Exception，不得捕获后返回伪造的空成功结果。
- Dev A 在 AI Harness 边界统一执行 `try/except`、超时和有限重试，将异常转换为系统错误码及任务状态并落库。
- Mock 实现与 Live 实现必须遵守完全相同的返回 Schema。

## 8. 数据对象

V0.5 使用九类核心对象：

| 对象 | 主要用途 | 必要关系 |
| --- | --- | --- |
| User | 飞书用户及工作空间身份 | 归属 Workspace |
| Source | 飞书或粘贴资料 | 可产生画像、经验或能力 |
| CustomerProfile | 一个客户的当前共享画像 | 一个客户一份，多会话复用 |
| Experience | 历史方案主要经验 | 关联一个 Source |
| Capability | PRD 原子能力 | 关联一个 Source |
| Session | 客户方案会话 | 关联一个 CustomerProfile |
| Message | 多轮用户与 AI 消息 | 关联一个 Session |
| SolutionRun | 一轮检索与生成快照 | 关联请求 Message |
| Job | 异步任务状态与重试信息 | 关联处理目标 |

通用规则：

- 所有业务数据必须带 `workspace_id` 或通过父对象确定工作空间。
- 经验和原子能力必须分别存储。
- 资产结构可使用 JSONB，但字段和必填规则由 Schema 固定。
- 删除采用软删除；软删除资产立即退出后续检索。
- `SolutionRun.retrieval_snapshot` 保存生成当时所需的资产名称、结构化内容、来源和边界信息，不依赖实时资产表回查。即使原资产之后被软删除，历史方案及其快照仍必须完整展示。
- 演示数据带 `is_demo=true`。

## 9. HTTP 接口清单

基础路径为 `/api/v1`，统一使用 JSON；登录态采用 HttpOnly Cookie。所有 `POST` 和 `PATCH` 写接口必须支持 `Idempotency-Key` 请求头，由 Dev A 在服务端保存请求指纹和首次响应并实施 24 小时幂等控制。同一工作空间、同一用户、同一 Key 与相同请求指纹重复提交时返回首次结果；同一 Key 对应不同请求指纹时返回 `IDEMPOTENCY_KEY_CONFLICT`。

### 9.1 身份

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/auth/feishu/start` | 获取飞书授权地址 |
| GET | `/auth/feishu/callback` | 处理授权回调并建立会话 |
| GET | `/me` | 获取当前用户与工作空间 |
| POST | `/auth/logout` | 退出登录 |

### 9.2 资料与任务

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/sources/import-link` | 导入飞书文档链接 |
| POST | `/sources/import-text` | 导入粘贴文本 |
| GET | `/sources` | 查询资料列表 |
| GET | `/sources/{source_id}` | 查询资料详情 |
| PATCH | `/sources/{source_id}` | 修改用途、标题或标签 |
| POST | `/sources/{source_id}/refresh` | 刷新飞书来源 |
| POST | `/sources/{source_id}/retry` | 重试失败处理 |
| GET | `/jobs/{job_id}` | 查询异步任务状态 |

### 9.3 客户画像

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/customer-profiles` | 创建客户及空画像 |
| GET | `/customer-profiles` | 查询画像列表 |
| GET | `/customer-profiles/{profile_id}` | 查询画像详情 |
| POST | `/customer-profiles/{profile_id}/generate` | 生成画像草稿 |
| PATCH | `/customer-profiles/{profile_id}` | 修改画像 |
| POST | `/customer-profiles/{profile_id}/confirm` | 确认画像 |

### 9.4 资产审核

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/experiences` | 查询经验列表 |
| GET | `/experiences/{experience_id}` | 查询经验详情 |
| PATCH | `/experiences/{experience_id}` | 修改经验 |
| POST | `/experiences/{experience_id}/review` | 通过或打回经验 |
| GET | `/capabilities` | 查询能力列表 |
| GET | `/capabilities/{capability_id}` | 查询能力详情 |
| PATCH | `/capabilities/{capability_id}` | 修改能力 |
| POST | `/capabilities/{capability_id}/review` | 通过或打回能力 |

### 9.5 会话与方案

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| POST | `/sessions` | 创建客户会话 |
| GET | `/sessions` | 查询会话列表 |
| GET | `/sessions/{session_id}` | 恢复会话与消息 |
| POST | `/sessions/{session_id}/turns` | 提交需求或追问 |
| GET | `/solution-runs/{run_id}` | 查询生成状态与结果 |

## 10. 通用响应与错误契约

成功响应：

```json
{
  "request_id": "req_xxx",
  "data": {}
}
```

失败响应：

```json
{
  "request_id": "req_xxx",
  "error": {
    "code": "MODEL_TEMPORARILY_UNAVAILABLE",
    "message": "模型服务暂时不可用",
    "retryable": true,
    "details": {}
  }
}
```

首批错误码包括：

- `AUTH_REQUIRED`
- `FEISHU_AUTH_EXPIRED`
- `FEISHU_PERMISSION_DENIED`
- `SOURCE_NOT_FOUND`
- `SOURCE_TYPE_UNSUPPORTED`
- `SOURCE_TEMPORARILY_UNAVAILABLE`
- `CONTENT_EMPTY`
- `VALIDATION_FAILED`
- `IDEMPOTENCY_KEY_CONFLICT`
- `MODEL_TEMPORARILY_UNAVAILABLE`
- `JOB_FAILED`
- `RATE_LIMITED`
- `INTERNAL_ERROR`

检索不到已校验经验或能力属于正常的空快照业务结果，不作为系统异常；方案生成器应返回资料缺口和建议追问。

## 11. 方案输出契约

每个方案固定包含八个区块：

1. 需求理解。
2. 初步建议。
3. 历史经验依据。
4. 原子能力组合。
5. 前置条件与风险。
6. 待确认信息。
7. 资料来源。
8. 建议追问。

每项内容必须标识以下边界之一：

| 类型 | 标识 | 规则 |
| --- | --- | --- |
| 历史事实 | `historical_fact` | 只能来自已审核经验 |
| 企业能力 | `enterprise_capability` | 只能来自已审核原子能力 |
| AI 推断 | `ai_inference` | 必须使用推测性表达 |
| 待确认信息 | `pending_confirmation` | 来源不足或客户未说明 |

无有效经验或能力时，系统仍可输出需求理解、缺口和建议追问，但不得补造案例、能力、客户名称或量化指标。

## 12. 非功能需求

### 12.1 性能

- 普通列表和详情接口 P95 小于 1 秒。
- 单份 5 万中文字符以内资料在 120 秒内形成草稿。
- 快速方案目标 15 秒内完成，最长 60 秒后返回可重试状态。
- 100 条消息以内的会话在 2 秒内恢复。

### 12.2 稳定性

- 飞书限流、网络超时和模型临时错误自动重试最多 2 次。
- 无权限、资源不存在、链接无效和 Schema 错误不自动重试。
- 服务重启后已落库资料、消息、任务和方案能够恢复。
- 相同 `Idempotency-Key` 24 小时内返回首次任务结果。
- Dev A 的 AI Harness 是 AI 异常捕获边界：捕获 Molly 算子抛出的异常，映射为统一错误码并更新 `Job` 或 `SolutionRun` 状态；AI 算子不得吞掉异常。

### 12.3 安全

- 飞书令牌和模型密钥不得返回前端或写入日志。
- 所有查询按工作空间隔离。
- 日志不记录完整客户资料和完整 Prompt。
- 模型输出必须通过 Schema 和引用校验后才可作为正式结果。

## 13. 开发顺序与交付物

### 阶段一：契约与脚手架

- 提交 `openapi.yaml`，覆盖 P0 路由、枚举、示例和错误码。
- 建立 FastAPI 项目、配置、健康检查和统一响应结构。
- 建立九类数据对象和首版迁移。
- Molly 提交 AI Schema、Mock AIEngine 和单元测试样例。

### 阶段二：资料与资产

- 实现 Mock 登录和文本导入。
- 实现 Source、Job、Experience、Capability 和 Profile 流程。
- 接入 AI 提取接口并完成草稿落库。
- 实现资产修改、通过和打回。

### 阶段三：会话与方案

- 实现 Session、Message 和 SolutionRun。
- 实现简单检索、检索快照和 AI Harness。
- 实现八区块方案、引用质检和多轮追问。
- 跑通成功链路和无依据边界链路。

### 阶段四：飞书与联调

- 用 Live 飞书适配器替换对应 Mock。
- 按登录、导入、任务、审核、画像、会话、方案顺序切换前端接口。
- 完成一套大陆网络可访问的演示部署。

## 14. 验收标准

- P0 接口全部写入 OpenAPI，并可用 Mock 数据调用。
- 至少一份历史方案和一份 PRD 完成导入、提取、审核和检索。
- 同一客户画像可被两个会话复用，消息不会串线。
- 同一会话支持至少三轮连续追问。
- 方案不超过 3 条经验和 5 条能力，每条均有匹配原因和来源。
- 所有历史事实和企业能力都能追溯到审核资产和原资料。
- 无依据场景稳定说明缺口，不生成虚假案例、能力或数字。
- 飞书临时失败、模型超时和数据为空均有明确状态及降级结果。
- Mock 与 Live 使用同一套 API 和 Schema，切换时前端无需修改业务代码。

## 15. 当前风险与待确认项

1. 前端当前 Mock 响应字段尚未提供，需要尽快对照并冻结 OpenAPI。
2. 比赛飞书应用的 OAuth 回调地址和文档读取权限尚未确认。
3. 模型供应商、模型名称、密钥和大陆网络可用性尚未确认。
4. 演示用历史方案、PRD 和客户会议纪要尚未最终确定。
5. AI Schema 必须由两名开发者共同确认，否则数据库、接口和 Prompt 容易出现字段分叉。
6. 当前排期紧张，应优先跑通 Mock 黄金流程，再替换真实飞书和模型能力。
