# 淘到宝引擎 V2 后端测试验收结果

测试日期：2026-08-11

测试分支：`v2_backbone`

基线：`8.11_backbone` / `fb0790aaf3c526002bb00b106e3f5b82170b8263`

## 1. 验收结论

V2 后台服务、公开情报/画像提案、招标响应矩阵和方案演练后端已完成本地功能验收，旧 V0.5/V1 回归无失败。云端部署与线上冒烟结果在本分支部署完成后补充。

## 2. 自动化测试

| 项目 | 命令或方式 | 结果 |
|---|---|---|
| 全量回归 | `pytest -q`，连接独立 pgvector PostgreSQL | `239 passed`，0 failed |
| V2 专项 | 情报、画像提案、招标矩阵、演练、运行后台、契约 | 8 项专项断言全部通过（包含于全量 239 项） |
| Ruff | `ruff check app tests` | 通过 |
| V2 迁移 Ruff | 检查 `alembic/env.py` 与 V2 migration | 通过 |
| Python 编译 | `python -m compileall -q app alembic` | 通过 |
| JavaScript 语法 | 抽取 `static/index.html` 主脚本并执行 `node --check -` | 通过 |
| OpenAPI | 解析 `docs/openapi-v2-incremental.yaml` 并逐项匹配 FastAPI mounted paths/methods | 通过 |
| Wheel | `uv build --wheel` | 通过，生成 `xianjintuan_engine-0.1.0-py3-none-any.whl` |

全量测试只有四条非失败警告：一条 Starlette/TestClient 弃用提示和三条既有 PPTX XML 元素真假值弃用提示。

## 3. 数据库迁移

测试数据库使用 `pgvector/pgvector:pg16`，执行：

1. 空数据库从首个 migration 升级到 `8b11c0d2a001 (head)`：通过；
2. V2 head 降级到 `509d9268f418`：通过；
3. 再次升级到 V2 head：通过；
4. `alembic check`：`No new upgrade operations detected`。

数据库最终包含 45 个 public tables（含 `alembic_version`），ORM 业务表为 44 个。

## 4. 本地真实 HTTP 冒烟

使用 Uvicorn、真实 PostgreSQL、Mock AI/飞书/展示服务完成以下 HTTP 链路：

1. Mock 飞书 OAuth 登录：通过；
2. 新建手工公开情报 SearchRun：`201`；
3. 查询情报条目并建立不可变快照：`200/201`；
4. 上传粘贴版招标要求并提取 2 个需求项：`201`；
5. 在没有企业内部依据时生成响应矩阵：2 项均为 `missing_evidence`，没有编造能力；
6. 创建、开始、回答并结束方案演练：全部 `200/201`，生成报告；
7. 统一运行后台：返回 Deep Research、情报、演练共 3 条任务；
8. 模型连接后台：返回 5 个连接；
9. AI 连接测试：返回明确的 `mock` 状态。

## 5. 安全与可信测试

- localhost、私网、链路本地和元数据地址会被公开来源 URL 校验拒绝；
- Office ZIP 限制条目数与解压后大小，PDF 限制页数，上传原始文件限制 5 MiB；
- 情报与内部经验/能力分开存储，不进入资产在线召回；
- 画像提案未经员工确认不会写入画像；确认后仍把长期画像置为待确认；
- 响应矩阵拒绝跨工作区、未校验、来源过期、来源版本变化或 Embedding 未完成的资产；
- `missing_evidence` 响应项不能审核为可承诺；
- 演练会识别绝对承诺、未说明证据和未暴露知识缺口；
- 所有新增业务查询按 `workspace_id` 隔离，POST/PATCH 要求 `Idempotency-Key`；
- 服务连接接口不返回 API Key 或飞书 Secret。

## 6. 冻结契约检查

- 根目录 `openapi.yaml`：本次未修改；它完整包含外部冻结 V0.5 的 26 条路径，并保留既有 V1.1 展示稿增量路径。
- `app/contracts/ai.py`：本次未修改方法名、参数或返回类型；仍然只有五个方法。
- V2 只新增 `docs/openapi-v2-incremental.yaml`，未增加第六个 `AIEngine` 方法。

## 7. 构建与待补线上证据

本地 Wheel 构建通过。本地 Docker 构建在拉取 `python:3.11-slim` 时因当前网络无法连接 Docker Hub 而中止，不是代码或 Dockerfile 错误。云端服务器具备既有镜像缓存和外网环境，部署时必须重新执行 Docker 构建并以容器健康检查作为最终发布证据。
