# V1.1 历史 PPT 风格映射与 Renderer 升级方案

## 1. 文档目标

本阶段不推翻 FactLedger、FactBinding、EvidenceGuard、SlidePlan、LayoutEngine 和双 Renderer 架构。
目标是解决当前测试暴露的问题：历史 PPT 已提取出颜色、字体、形状与坐标，但进入 Renderer 后只剩基础色板、字体和
固定 `layout_token`，导致不同参考稿生成的页面只是“换色换字体”，结构和组件皮肤几乎完全一致。

目标链路：

```text
历史 PPTX
  -> SafePPTXParser（只读取视觉白名单）
  -> StyleFeatureSet（脱敏原始视觉统计）
  -> StyleProfileBuilder（聚类、归一化、置信度）
  -> StyleProfile v2（DesignTokens + ComponentSkins + LayoutArchetypes）
  -> CandidateTemplate Preview（候选模板预览与人工确认）
  -> TemplateCompiler（编译为受限 LayoutTemplate）

FactLedger -> Molly SlidePlan -> Materializer -> EvidenceGuard 预检
  -> ProfileScoped LayoutEngine -> PositionedSpec -> EvidenceGuard 终检
  -> RenderIR（跨 Renderer 的唯一视觉中间表示）
  -> HTMLRenderer / PPTXRenderer
  -> Playwright / PowerPoint 视觉门禁
```

## 2. 当前问题与根因

三个真实样本的技术验收均通过，但人工查看 PowerPoint PNG 后视觉质量不通过：

- 卡片面积过大，内容集中在中部，留白失衡。
- 标题字号直接按历史基础字号比例放大，部分达到不合理尺寸。
- 正文统一垂直居中，没有卡片标题、正文、来源脚注的层级。
- 边框统一使用辅助色，未学习历史 PPT 的圆角、粗细、填充与装饰规律。
- 三份参考稿虽提取出不同布局模式，最终仍落到同一套固定 `two_column` 坐标。
- `layout_grammar` 是字符串，Renderer 无法可靠消费其中的坐标、频率和置信度。

根因不是“已有 Renderer 不能使用”，而是提取结果与 Renderer 中间缺少结构化编译层。

## 3. 不可破坏的边界

1. StyleProfile 只携带视觉和叙事规则，不携带历史 PPT 的业务文本、数字或客户名称。
2. AI 只能输出 SlidePlan，不输出 HTML、CSS、PPTX、绝对坐标、颜色或字体。
3. TemplateCompiler 与 Renderer 均为确定性代码，不调用 AI、数据库或网络。
4. 历史几何特征只能编译为白名单模板，不能直接成为任意自由坐标代码。
5. TemplateCompiler 不创建、删除、合并或改写事实组件。
6. HTML 与 PPTX 必须消费同一份 PositionedSpec 和同一份 CompiledStyle。
7. 布局前检查 FactBinding，布局后重新检查事实指纹；视觉优化不能修改任何事实文本和数字。

## 4. 开源代码采用策略

### 4.1 可直接移植或改造

#### aiden0z/pptx-renderer

重点复用：

- `RenderContext` 的页面尺寸、坐标与继承上下文思想。
- `TextRenderer` 的文本框内边距、段落、行距、对齐、字号与字体映射。
- `ShapeRenderer` 的填充、边框、圆角和透明度映射。
- Theme、Master、Layout、Placeholder 的继承解析；当前项目已有一部分 Python 移植。

迁移要求：保留 Apache-2.0 notice；只移植视觉计算，不移植历史业务正文读取和展示逻辑。

#### Presenton

重点复用：

- Template、Layout、Element 的分层模型。
- 受限模板注册和 Schema 到 HTML Fragment 的分发模式。
- 模板容量检查和渲染前验证。

迁移要求：将自由内容字段替换为当前项目的 SemanticComponent 和 FactBinding，不采用模型生成页面代码的部分。

#### Presentation AI

继续复用当前已移植的 OOXML relationship、`clrMap`、Theme Palette、Font Scheme 算法，并补充主题字号层级、
shape fill/line 和 layout usage frequency 的结构化产出。

#### PptxGenJS

继续作为 PPTX 写入引擎，复用其文本、Shape、表格和图片的确定性 API。PptxGenJS 只负责写 OOXML，不能承担
模板选择、事实组织和内容改写。

### 4.2 只参考设计、不直接复制

- SeaSlides 的 Strategist -> Executor -> Renderer 分层和 Build Feedback。
- DeepSlides 的 Content -> Slide Design -> Implementation 分层。
- 任何让 LLM 输出自由 HTML/CSS、任意坐标、数字或业务陈述的实现。
- 任何允许上传可执行模板、JavaScript 或任意外部图片 URL 的实现。

## 5. 新增和调整的文件

```text
app/schemas/style_profile.py
app/presentation/style/features.py
app/presentation/style/profile_builder.py
app/presentation/style/token_normalizer.py
app/presentation/style/archetype_clusterer.py
app/presentation/style/template_compiler.py
app/presentation/style/font_resolver.py
app/presentation/rendering/component_skins.py
app/presentation/rendering/text_fit.py
app/presentation/rendering/compiled_style.py
app/presentation/rendering/render_ir.py
app/presentation/rendering/render_ir_builder.py
app/presentation/rendering/render_ir_validator.py
app/presentation/rendering/font_metrics.py
app/presentation/style/template_candidates.py
app/services/style_template_review_service.py
app/presentation/visual/diagnostics.py
app/presentation/visual/powerpoint_export.py
tests/golden/presentation_styles/
tests/test_style_profile_builder.py
tests/test_template_compiler.py
tests/test_component_skins.py
tests/test_render_ir.py
tests/test_renderer_style_fidelity.py
scripts/validate_pptx_samples.py（继续扩展）
```

现有文件调整：

- `app/services/pptx_parser.py`：返回结构化 StyleFeatureSet，不再只产生字符串 grammar。
- `app/services/pptx_sanitized_visual.py`：补充形状类型、填充、边框、对齐和字号角色的安全统计。
- `app/presentation/layouts/registry.py`：支持以 StyleProfile 为作用域的模板注册表。
- `app/presentation/layouts/engine.py`：消费编译后的模板，但仍只分配组件，不改写内容。
- `app/services/html_renderer.py`：消费 CompiledStyle 和 ComponentSkin。
- `sidecar/pptx-renderer/render.mjs`：与 HTML 共用 token/skin 语义，补齐对齐、内边距和文本层级。

## 6. StyleFeatureSet 设计

StyleFeatureSet 是解析器内部的脱敏统计，不直接对外展示：

```text
StyleFeatureSet
  canvas: width, height, aspect_ratio
  color_samples: color, role, frequency, page_coverage
  font_samples: family, size_pt, weight, role, frequency
  shape_samples: kind, normalized_geometry, fill, line, radius_hint
  page_samples:
    normalized_boxes
    title_band
    column_count
    card_grid
    image_region
    density
  master_layouts: placeholder roles and normalized geometry
```

安全规则：

- 不保存 `<a:t>`、图表值、备注、OLE、嵌入对象或链接目标。
- 坐标统一归一化到 0-10000。
- 颜色和字号按频率统计，不保留其原始文本上下文。
- page sample 只保存 `shape_1/shape_2` 等匿名标识。

## 7. StyleProfile v2 设计

### 7.1 DesignTokens

```text
DesignTokens
  colors:
    canvas, surface, primary, secondary, accent, text_primary, text_muted, border
  typography:
    display, title, subtitle, body, evidence, footnote
    每级包含 font_family, size_pt, weight, line_spacing, color_token
  spacing:
    page_margin, section_gap, component_gap, card_padding
  shape:
    radius, border_width, border_style, shadow, surface_fill
  decoration:
    title_rule, accent_bar, page_number, header_footer
```

字号不能无限继承历史值，必须经过安全范围归一化：

- display：32-48 pt
- title：26-38 pt
- subtitle：20-28 pt
- body：16-24 pt
- evidence：14-20 pt
- footnote：10-14 pt

保留历史 PPT 的层级比例，但超出范围时整体缩放，禁止出现当前测试中的超大标题。

### 7.2 ComponentSkins

```text
ComponentSkin
  token
  applies_to
  fill_token
  border_token / border_width
  radius
  padding
  heading_style
  body_style
  alignment
  vertical_alignment
  accent_placement
```

第一批冻结：`plain_text`、`outline_card`、`filled_card`、`accent_bar_card`、`metric_focus`、
`comparison_split`、`timeline_nodes`、`process_steps`、`source_footer`。

正文默认顶部对齐；只有指标和封面允许垂直居中。卡片标题与正文必须分开创建文本框，不能再拼成一个字符串。

### 7.3 LayoutArchetype

```text
LayoutArchetype
  archetype_token
  purpose
  frequency
  confidence
  slots:
    role, normalized_geometry, allowed_components, capacity
  preferred_skin_tokens
  fallback_archetype
```

MVP 白名单：

- `cover_centered`
- `cover_left_aligned`
- `title_body`
- `title_two_column`
- `title_three_cards`
- `metric_with_evidence`
- `comparison_split`
- `timeline_horizontal`
- `process_horizontal`
- `evidence_grid`
- `source_list_compact`

历史 PPT 的几何聚类只能映射到这些白名单 archetype。无法可靠匹配时回退到系统模板，不允许自由执行历史坐标。

## 8. TemplateCompiler

TemplateCompiler 是本阶段最关键的新层：

```text
StyleProfile v2 + system archetype registry
  -> 选择置信度足够的 LayoutArchetype
  -> 将匿名历史 slot 映射为 title/body/card/image/evidence 角色
  -> 应用画布边距和最小间距
  -> 校验容量、重叠、越界和最小字号
  -> 产生只读 CompiledLayoutTemplate
```

编译失败处理：

1. 尝试同 archetype 的安全坐标修正。
2. 切换到该 archetype 的系统 fallback。
3. 仍失败则标记 `STYLE_TEMPLATE_UNUSABLE`，禁止进入 Renderer。

TemplateCompiler 只编译视觉模板，不读取 FactLedger，也不接触组件文字。

## 9. Renderer 升级

### 9.1 共享渲染语义

HTML 和 PPTX 不再分别解释 ComponentSkin，而是共同消费冻结后的 RenderIR：

```text
PositionedSpec
CompiledStyle
  -> RenderIRBuilder
  -> RenderIR
       canvas
       layers
       TextNode / TextRun
       ShapeNode / ImageNode / TableNode / VerifiedChartNode
       geometry / padding / alignment / fill / stroke / shadow / clipping
       fact_trace / asset_trace
       renderer_contract_version
```

RenderIR 是 Renderer 唯一允许消费的输入。HTMLRenderer 和 PPTXRenderer 只能将同一节点映射到各自运行时，
不能再次选择模板、解释 StyleProfile、计算业务布局或修改文字。该设计直接借鉴 Presenton Template v2 中
`TextRun/Font/Fill/Stroke/Shadow/Padding/Alignment/Flex/Grid` 的场景模型，但删除自由业务内容入口，
为事实节点增加 `fact_trace`，为资源节点增加 `asset_trace`。

每份 RenderIR 必须包含 SHA-256 内容哈希、PositionedSpec 哈希、CompiledStyle 哈希和版本号。两个 Renderer
分别记录消费的 RenderIR 哈希，以此证明 HTML/PPTX 来自同一视觉快照。

### 9.2 文本渲染

- 标题、正文、来源分别建立文本框。
- 第一阶段可以用字符预算做快速预检，但正式布局必须使用真实字体度量：HTML 侧使用隔离 measurement DOM，
  PPTX 侧使用字体文件的 glyph metrics 与 PowerPoint 转图回归。直接借鉴 `pptx-renderer/TextRenderer.ts`
  的七级样式继承、段落间距、绝对/比例行距和隐藏测量根节点算法。
- 中文排版增加 CJK 禁则、标点挤压、长数字不可拆分、中英文混排和字体 fallback 后重新测量。
- 溢出处理顺序：缩小非关键间距 -> 字号在允许范围内回退 -> fallback 模板 -> 分页。
- 禁止通过删字、改写数字或让 AI 重写来解决溢出。
- 字体缺失时按可配置 fallback chain 替换，并写入 RenderDiagnostics。

### 9.3 Shape 与装饰

- 形状填充、边框、圆角、阴影均来自 ComponentSkin 白名单。
- 装饰线和色块不承载事实，不进入 FactBinding，但必须位于组件层下方且不能遮挡正文。
- 不直接复制历史 PPT 图片、Logo 或业务图形；品牌资产后续通过审核资源 ID 单独接入。

## 10. AI、Molly 与 Harness 边界

本阶段不新增 Molly 输出字段。Molly 仍只输出：

- purpose
- layout_token / archetype preference
- component_type
- claim_ids
- priority
- character_budget
- allow_pagination

Harness 继续负责超时、重试、Schema 校验和 Mock/Live 切换。TemplateCompiler、ComponentSkin 选择和 Renderer
全部由后端确定性代码完成，Molly 无权输出颜色、字体、坐标、HTML、CSS 或 PPTX。

## 11. 数据库与 API

R1-R2 可以暂不新增表，继续使用 `VisualStyleProfile.visual_json` 验证 Schema 与编译算法，但进入企业联调前
必须将“用户确认过的模板版本”和“实际渲染快照”变成不可变审计对象。

第一阶段 `visual_json` 增加：

```text
schema_version: style-profile-v2
feature_summary
design_tokens
component_skins
layout_archetypes
confidence_report
compiler_version
```

旧 v1 profile 通过适配器补默认 skin 和系统 archetype，保证历史数据可用。确认后的 StyleProfile 仍不可原地覆盖，
修改时创建新版本并保留旧 Presentation 的快照。

企业联调前新增：

```text
StyleTemplateVersion
  style_profile_id / version / status
  source_deck_hashes
  feature_set_hash / compiled_template_hash
  compiler_version / confirmed_by / confirmed_at
  compiled_template_json / confidence_report

PresentationRenderSnapshot
  presentation_id / version
  positioned_spec_hash / compiled_style_hash / render_ir_hash
  render_ir_json / renderer_versions
  font_substitution_report / diagnostics / artifact_hashes
```

这两类记录可以独立建表，也可以先落在已有版本化 Artifact 表中；无论物理实现如何，业务上必须不可变、可追踪、
可回滚。只保存最终 HTML/PPTX 文件路径不满足企业审计要求。

API 保持现有生成路由，并补充候选模板确认能力：

```text
GET  /style-profiles/{id}/template-candidates
POST /style-profiles/{id}/template-candidates/{candidate_id}/confirm
POST /style-profiles/{id}/template-candidates/regenerate-preview
```

候选模板必须先生成脱敏示例内容预览；用户确认后才允许生成正式客户方案。`GET /style-profiles/{id}` 增量返回
`schema_version`、`confidence_report`、`confirmed_template_version` 和预览图。

## 12. 分阶段实施

### R1：Golden 基线与验收门禁

- test1/test2/test3 作为第一批本地冒烟样本，不提交含业务数据的原 PPT。
- 企业联调前扩展到 30-50 套脱敏参考稿、100-200 张 Golden 页面，覆盖快消、汽车、金融、医疗、政企等
  不同行业以及封面、章节、指标、对比、时间线、流程、证据和来源页面。
- 提交脱敏 StyleFeatureSet fixture、目标 StyleProfile 和安全示例 PositionedSpec。
- 扩展现有脚本，输出 HTML/PPTX、PowerPoint PNG 和结构化 diagnostics。
- 建立人工评分表：风格相似、层级、留白、可读性、客户可展示性。

验收：基线可重复，所有失败都有截图和机器可读原因。

### R2：StyleProfile v2 与 TemplateCompiler

- 将字符串 grammar 替换为强类型 FeatureSet 与 LayoutArchetype。
- 实现坐标归一化、布局聚类、置信度和 fallback。
- 每个 archetype 生成 1-3 个候选模板和脱敏预览，只有用户确认的候选项进入 ProfileScoped LayoutRegistry。
- 记录候选来源、匹配分数、异常页剔除原因和人工选择结果，后续可用于改进聚类算法。

验收：三个样本至少产生两种不同 archetype；同一输入重复编译结果完全一致。

### R3：ComponentSkin 与文本排版

- 先冻结 RenderIR v1，再移植开源 TextRenderer/ShapeRenderer 中可复用算法。
- 标题、正文、来源拆框；补字体回退、字号范围、行距、padding、边框和对齐。
- HTML 与 PPTX 使用同一 token 映射测试。

验收：当前三张测试图中的超大标题、垂直居中正文和空卡片问题全部消失。

### R4：业务模板覆盖

- 覆盖 cover、two-column、three-card、metric、comparison、timeline、process、evidence、sources。
- 增加确定性分页和模板 fallback。
- 接入审核品牌资产：Logo、品牌底纹和图片必须通过 `asset_id`、权限快照、哈希和 MIME 白名单引用。

验收：10 类页面均无重叠、越界、截断和事实变化。

### R5：视觉闭环与人工评审

- HTML 使用 Playwright 三视口截图。
- PPTX 使用 PowerPoint；CI/服务器优先补 LibreOffice headless。
- 自动检查页数、非空白、边界、重叠、最小字号、对比度和事实原文。
- 保存人工评分和失败原因，反馈只能调整 style/layout，不得修改 FactLedger。

人工盲评采用五项 1-5 分量表：风格相似度、信息层级、留白、可读性、企业品牌感。单页平均分至少 4.0，
任一维度不得低于 3.5；“无需设计师修改即可展示”的页面比例至少 70%。该指标必须同时报告样本量、行业分布和
95% 置信区间，不能只报告一个百分比。事实安全指标始终保持 100%。

## 13. 测试矩阵

### 单元测试

- XML 脱敏：敏感文本、数字、备注、图表值和 OLE 不进入 FeatureSet。
- Token 归一化：极端字号、颜色和间距被限制在安全范围。
- Archetype 聚类：同类布局稳定聚合，不同布局不会错误合并。
- TemplateCompiler：未知 archetype、重叠、越界和容量超限 fail closed。
- ComponentSkin：只允许白名单属性，不接受 CSS、JS 或任意路径。
- TextFit：中英文、长数字、长单词和多来源脚注均不溢出。

### 集成测试

- `PPTX -> StyleProfile v2 -> CompiledTemplate -> HTML/PPTX` 全链路。
- HTML/PPTX 页数、组件数量、事实原文、Claim/Source 追踪一致。
- 同一输入重复生成的 Schema、坐标和 OOXML 事实内容一致。
- v1 StyleProfile 兼容适配。

### 视觉测试

- 1440x900、1366x768、390x844 HTML 预览。
- 1600x900 PowerPoint PNG。
- 页面非空、无滚动、无重叠、无越界、无截断。
- 正文不小于 16 pt，来源不小于 10 pt；标题不超过 48 pt。
- 人工检查风格相似度、信息层级、留白与企业感。

### 事实安全回归

- 未知 Claim/Evidence/Source 拦截率 100%。
- 数字和 verbatim 文本篡改拦截率 100%。
- Style 编译与 Renderer 前后事实指纹一致率 100%。
- 历史 PPT 业务文本进入 StyleProfile 的数量为 0。

## 14. MVP 暂不实现

- 任意历史 PPT 像素级复刻。
- 直接复制历史图片、Logo、图表、SmartArt 和业务正文；但 R4 必须支持独立审核后的品牌资产 ID。
- AI 生成或修改 CSS、HTML、坐标和数字。
- 自由拖拽编辑器和用户上传可执行模板。
- 动画、视频、3D 和复杂交互。
- 任意外部字体下载和图片 URL。
- 无审核品牌资产自动进入生成稿。

## 15. 分工建议

### Dev A

- StyleProfile v2、TemplateCompiler、ProfileScoped LayoutEngine。
- HTML/PPTX Renderer、ComponentSkin、TextFit、诊断和视觉流水线。
- API 快照兼容、任务状态和开发文档。

### Molly

- 保持 SlidePlan 契约，补 archetype preference 的专项评测。
- 验证 Planner 在不同 StyleProfile 下选择合适页面目的和组件组合。
- 不负责坐标、皮肤和事实文本。

### 组长 / Harness

- 冻结跨模块契约、统一 trace、超时和错误码。
- 确认 Demo 故事线、人工评分标准和“可直接展示”门槛。
- 组织 Dev A 与 Molly 的集成回归。

## 16. 推荐下一步

下一次编码只做 R1 和 R2，不同时大改 Renderer：

1. 冻结 StyleFeatureSet、DesignTokens、ComponentSkin、LayoutArchetype 和 RenderIR Schema。
2. 把现有字符串 `layout_grammar` 升级为强类型内部结构。
3. 用 test1/test2/test3 生成可审查的 v2 profile 和 archetype 聚类报告。
4. 实现 TemplateCompiler，并证明三个样本能编译出不同、安全、可回退的 LayoutTemplate。
5. 为每个样本生成候选模板预览并要求人工确认，禁止未确认模板进入正式生成。
6. R2 验收后再进入 TextRenderer/ShapeRenderer 移植，避免提取层和渲染层同时变化导致无法定位问题。

## 17. 成熟开源实现对齐清单

### Presenton（Apache-2.0，审查版本 b940f7b）

直接借鉴：

- `servers/fastapi/templates/v2/models/elements.py`：Position、Size、Padding、Font、Fill、Stroke、Shadow、
  TextRun、Container、Flex、Grid、Group、Table、Chart 等 RenderIR 基础模型。
- `servers/fastapi/templates/v2/models/layouts.py`：Layout、Component、Variant 和 ID 唯一性约束。
- `servers/fastapi/templates/v2/schema.py`：从模板提取受限内容 Schema、`additionalProperties=false` 和容量约束。
- `servers/nextjs/lib/template-v2-json-to-html.ts`：固定 1280x720 画布、元素白名单、溢出隐藏和 HTML 分发。
- `servers/fastapi/templates/pptx_font_utils.py`：字体家族归一化、Theme 字体引用、字体元数据和授权信息提取。

禁止复制：Layout Code 自由执行、未绑定事实的自由内容生成、任意外部字体/图片下载。Presenton 自身仍为
0.9.4-beta，因此采用的是分层模型和经过测试的算法，不把其整体产品成熟度当作本项目质量证明。

### aiden0z/pptx-renderer（Apache-2.0，当前已固定 revision）

直接借鉴：

- `RenderContext.ts`：slide -> layout -> master -> theme 完整解析上下文、字体集合、缓存和测量根节点。
- `TextRenderer.ts`：七级样式继承、段落、行距、项目符号、CJK/数字 run 处理和字体栈。
- `ShapeRenderer.ts`、`StyleResolver.ts`：Fill、Line、Gradient、Shadow、Geometry 和安全外部资源解析。

禁止复制：读取并复现历史业务正文、超出 StyleFeatureSet 白名单的媒体和链接。该项目解决的是原 PPT 忠实显示，
不是新内容生成，因此算法必须落入 RenderIR 和 FactBinding 边界后才能使用。

### Presentation AI（MIT，当前已固定 revision）

继续复用 OOXML relationship、Theme Palette、Font Scheme、`clrMap` 和颜色 modifier 算法。参考其 Custom Theme
持久化与人工调整流程，但不引入用户系统、图片生成和无证据内容。

### PptxGenJS（MIT）

只作为最终 OOXML Writer。固定版本、锁定依赖、维护 SBOM，并持续跟踪当前 `image-size` 间接依赖的安全公告。
开放图片组件前必须完成依赖升级、图片像素/体积限制、解码超时和资源哈希校验。

### 许可证与供应链要求

- 所有实际移植代码记录仓库、commit、源文件、目标文件和修改说明。
- Apache-2.0/MIT notice 进入 `THIRD_PARTY_NOTICES.md`，构建产物携带许可证清单。
- CI 执行 Python 与 npm 依赖漏洞扫描、SBOM 生成和锁文件校验。
- 不直接复制无法确认许可证、来源或固定 revision 的代码。
