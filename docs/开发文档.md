# 淘到宝引擎 V0.5 开发文档

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 淘到宝引擎 |
| 仓库 | `https://github.com/wxh042/xianjintuan-engine` |
| 当前开发分支 | `feat/gateway` |
| 文档版本 | V0.1 |
| 建档时间 | 2026-08-06 |
| 维护责任 | Dev A；所有参与开发者在代码或契约变更后同步更新 |

## 1. 文档目的

本文是淘到宝引擎 V0.5 的持续开发记录，说明项目当前状态、架构决策、已完成工作、文件变更、运行方式、验证结果、已知问题和后续计划。

从 2026-08-06 起，本文作为开发交付的一部分维护。任何接口、数据结构、业务流程、配置、依赖、测试或部署操作，都必须在本文留下可追溯记录。

## 2. 强制开发记录规范

### 2.1 每项开发任务必须记录

每次开发至少记录以下内容：

1. 日期和执行人。
2. 任务目标及对应需求。
3. 操作前状态或前置条件。
4. 实际执行步骤。
5. 新增、修改或删除的文件。
6. 关键接口、数据结构或架构决策。
7. 执行的验证命令及结果。
8. 未完成内容、已知风险和下一步。

### 2.2 文档更新时机

- 开始开发前：在“进行中任务”中登记目标和范围。
- 开发过程中：接口或架构发生变化时立即同步文档。
- 完成开发后：补充文件清单、验证结果和遗留问题。
- Git 提交前：确认开发文档与代码状态一致。
- PR 合并前：确认 OpenAPI、测试和开发文档同时更新。

### 2.3 禁止事项

- 禁止只修改代码而不更新开发文档。
- 禁止接口字段变化后不更新 `openapi.yaml`。
- 禁止将未执行的测试写成“已通过”。
- 禁止将 Mock 能力描述成已经接通真实服务。
- 禁止在文档、代码或日志中记录令牌、Cookie、密钥和客户敏感原文。

### 2.4 单次操作记录模板

后续每项任务在第 10 节按以下模板追加：

```markdown
### YYYY-MM-DD / DEV-NNN：任务名称

- 执行人：
- 目标：
- 需求依据：
- 操作前状态：
- 操作步骤：
  1. ...
- 文件变更：
  - `path/to/file`：变更说明
- 验证：
  - `command`：通过/失败及关键结果
- 架构或接口决定：
- 未完成与风险：
- 下一步：
```

## 3. 当前产品范围

V0.5 目标是跑通以下黄金流程：

```text
飞书登录或 Mock 登录
  -> 导入飞书文档或粘贴文本
  -> 提取客户画像、经验或原子能力草稿
  -> 人工修改和审核
  -> 创建客户会话并输入需求
  -> AI 提取结构化检索意图
  -> 数据库过滤、排序并形成 Top-K 检索快照
  -> AI 基于快照生成八区块方案
  -> 引用和事实边界校验
  -> 保存结果并支持多轮追问
```

当前阶段不实现向量数据库、Multi-Agent、Deep Research、知识图谱、自动建群、PPT 生成和复杂微服务基础设施。

## 4. 职责边界

### 4.1 Dev A

- FastAPI 路由、请求响应和全局错误处理。
- OpenAPI 契约和前后端联调。
- PostgreSQL 模型、迁移、Repository 和事务。
- 飞书 OAuth、资料读取及 Mock/Live 适配。
- 后台任务、幂等、重试和状态落库。
- AI Harness 运行编排、异常转换、引用校验和结果保存。
- 黄金流程测试及演示部署。

### 4.2 Molly

- AI 结构化 Schema。
- 客户画像、经验和能力提取算子。
- `SearchIntent` 提取算子。
- 方案生成 Prompt 与生成算子。
- 引用、边界和防编造质检逻辑。

### 4.3 AI 与服务端边界

- AI 不接收全量经验或能力列表。
- AI 通过 `extract_search_intent(context)` 返回结构化检索意图。
- Dev A 在数据库层完成工作空间隔离、状态过滤、硬约束过滤、排序和 Top-K 截断。
- Dev A 只向 `generate_solution` 传入最多 3 条经验和 5 条能力的固化快照。
- Molly 的算子遇到 LLM 错误时直接抛出 Python Exception。
- Dev A 在 AI Harness 捕获异常，执行重试、错误码转换和任务状态落库。

## 5. 当前目录结构

```text
.
├── README.md
├── main.py
├── openapi.yaml
├── pyproject.toml
├── .env.example
├── app/
│   ├── main.py
│   ├── api/v1/
│   │   ├── router.py
│   │   └── routes/health.py
│   ├── contracts/ai.py
│   ├── core/
│   │   ├── config.py
│   │   ├── errors.py
│   │   ├── exception_handlers.py
│   │   ├── idempotency.py
│   │   ├── middleware.py
│   │   └── responses.py
│   ├── db/
│   │   ├── database.py             # Async Engine、Session Factory 与事务上下文
│   │   ├── models.py               # SQLAlchemy 2.0 九类核心模型
│   │   └── repositories/           # 泛型 DAO 与九实体 Repository
│   ├── integrations/
│   └── services/
├── docs/
│   └── 开发文档.md
└── tests/
    ├── test_database.py
    ├── test_health.py
    ├── test_models.py
    └── test_repositories.py
```

## 6. 已实现基础能力

### 6.1 FastAPI 应用入口

- 根目录 `main.py` 导出 ASGI `app` 和应用工厂 `create_app`。
- `app/main.py` 创建 FastAPI 应用、注册中间件、异常处理器和 V1 路由。
- 本地启动入口为 `uvicorn main:app --reload`。

### 6.2 统一响应

成功响应：

```json
{
  "request_id": "req_xxx",
  "data": {}
}
```

失败响应：

```json
{
  "request_id": "req_xxx",
  "error": {
    "code": "INTERNAL_ERROR",
    "message": "服务暂时不可用",
    "retryable": true,
    "details": {}
  }
}
```

### 6.3 Request ID

- 优先透传请求 Header 中的 `X-Request-ID`。
- 请求未提供时生成 `req_<uuid>`。
- 响应体和 `X-Request-ID` 响应 Header 使用同一个值。

### 6.4 全局异常处理

- `AppError`：保留业务错误码、HTTP 状态、是否可重试和详情。
- `RequestValidationError`：转换为 `VALIDATION_FAILED`。
- 未处理 Exception：记录服务端日志，对外转换为 `INTERNAL_ERROR`，不泄漏异常堆栈。

### 6.5 幂等基础约束

- 所有 POST/PATCH 在 OpenAPI 中要求 `Idempotency-Key`。
- Key 长度为 16 至 128 个字符。
- 同一工作空间、用户和 Key 的幂等有效期为 24 小时。
- 同 Key、同请求指纹返回首次响应。
- 同 Key、不同请求指纹返回 `IDEMPOTENCY_KEY_CONFLICT`。
- 当前仅实现 Header 校验依赖，数据库持久化将在数据层阶段实现。

### 6.6 健康检查

当前接口：`GET /api/v1/health`。

当前返回：

```json
{
  "request_id": "req_xxx",
  "data": {
    "status": "ok",
    "service": "ok",
    "database": "not_configured",
    "ai": "mock"
  }
}
```

这表示 FastAPI 服务可运行，但数据库尚未配置，AI 当前为 Mock 模式。

## 7. OpenAPI 契约状态

当前 `openapi.yaml`：

- OpenAPI 版本：3.1.0。
- 基础路径：`/api/v1`。
- P0 路径数量：26。
- POST/PATCH 写操作数量：16。
- 缺少 `Idempotency-Key` 的写操作：0。
- 本地 `$ref` 数量：248。
- 无法解析的本地 `$ref`：0。
- 5 个列表响应均为单层 `data.items`，并包含 `has_more`。
- `JobStatus` 与 `RunStatus` 均为 `pending/running/completed/failed`。
- 所有已命名业务 ID 均引用 UUID 格式约束。

契约包含：

- 飞书授权、当前用户和退出登录。
- 飞书链接和文本资料导入。
- 资料列表、详情、修改、刷新和重试。
- 后台任务状态。
- 客户画像创建、列表、详情、生成、修改和确认。
- 经验和原子能力查询、修改及审核。
- 会话创建、列表、恢复和多轮消息。
- 方案运行轮询和八区块方案结构。
- 统一错误码、分页、状态枚举和健康检查。

注意：`openapi.yaml` 是目标 P0 契约，不代表其中所有接口都已经有路由实现。目前实际挂载的业务接口只有健康检查。

## 8. 本地开发与验证

### 8.1 环境要求

- Python 3.11 或更高版本。
- PowerShell。
- 后续数据库阶段需要 PostgreSQL。

### 8.2 初始化

```powershell
cd "D:\神州"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev]"
```

`.venv/` 已写入 `.gitignore`，不得提交虚拟环境。

### 8.3 启动

```powershell
.\.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

访问地址：

- Swagger UI：`http://127.0.0.1:8000/docs`
- 健康检查：`http://127.0.0.1:8000/api/v1/health`

### 8.4 验证命令

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\ruff.exe check app main.py tests
python -m compileall -q app main.py tests
git diff --check
```

## 9. 当前状态与已知限制

### 9.1 已完成

- 需求和职责边界梳理。
- Git 仓库、远程地址和三分支协作结构。
- AI Harness 第一版架构修正。
- P0 OpenAPI 初稿。
- FastAPI 应用骨架。
- Request ID、统一响应和全局异常处理。
- 幂等 Header 校验骨架。
- 健康检查及自动化测试。
- 本地虚拟环境和开发依赖安装。
- SQLAlchemy 2.0 九类核心模型和异步数据库工厂。

### 9.2 未完成

- Alembic 迁移。
- 24 小时幂等记录的数据库实现。
- 登录、资料、画像、资产、会话和方案的实际路由。
- 后台任务工作器及任务恢复。
- 飞书 Mock/Live 适配器。
- AI Engine Mock 及 Molly 的真实算子。
- 数据库检索、Top-K 快照和引用质检。
- 前端联调和演示部署。

### 9.3 当前风险

- Molly 的最终 Pydantic Schema 尚未合入，`app/contracts/ai.py` 暂时使用 `dict` 占位。
- OpenAPI 需要与前端现有 Mock 字段逐项对照。
- 飞书应用权限、回调地址和演示资料尚未确认。
- 模型供应商、模型密钥及大陆网络可用性尚未确认。
- 当前只有健康检查实现，不能将目标 OpenAPI 视为已完成的业务服务。

## 10. 开发操作记录

### 2026-08-06 / DEV-016：发布团队开发与协作文档

- 执行人：Codex，协助 Dev A。
- 目标：将开发日志和 Molly 对接说明从个人资料调整为 GitHub 团队共享资料。
- 操作前状态：两份文档已在本地维护，但被 `.gitignore` 排除，合作者无法通过仓库查看。
- 实际操作：
  1. 扫描两份文档中的密码、Token、Secret、数据库 URL 和本地网络信息。
  2. 确认没有实际密码、API Key、Cookie 或访问令牌；仅保留非敏感的本地开发风险说明。
  3. 从 `.gitignore` 删除两份文档的排除规则。
  4. 将 `docs/开发文档.md` 和 `docs/Molly_GitHub协作说明.md` 纳入版本控制并推送到 `feat/gateway`。
- 共享规范：后续代码、接口、依赖、数据库、部署和协作流程变化必须同步更新开发文档；禁止在文档中提交真实密钥。
- 文件变更：`.gitignore`、`docs/开发文档.md`、`docs/Molly_GitHub协作说明.md`。

### 2026-08-06 / DEV-015：更新 Molly AI Harness 对接说明

- 执行人：Codex，协助 Dev A。
- 目标：向组长和 Molly 同步已完成代码，并冻结 AI Engine 与 Gateway 的对接边界。
- 操作前状态：`feat/gateway` 已推送认证模块，但旧 Molly 说明仍停留在 `4d5b860`，未包含数据库、适配器和认证基础设施。
- 实际操作：
  1. 通过 `git ls-remote` 确认 GitHub `feat/gateway` 最新提交为 `853e7db`。
  2. 更新可直接发送到群里的 @Molly 同步话术。
  3. 更新 Molly 首次同步命令、分支职责和 PR 流程。
  4. 列出双方百分百复用的 OpenAPI、配置、数据库、Repository、适配器、鉴权和错误处理设施。
  5. 固化五个 AI 算子、两阶段检索、Top 3/Top 5 快照和异常传播规则。
  6. 增加双方交付物及第一次 Harness 联调验收清单。
- 文件变更：
  - `docs/Molly_GitHub协作说明.md`：全量更新至 `853e7db` 基线。
  - `docs/开发文档.md`：新增本条操作记录。
- 发布边界：上述两个文件均为内部协作文档，受 `.gitignore` 保护，不上传 GitHub；通过飞书群或本机文件发送给 Molly。
- 验证：远端 `refs/heads/feat/gateway` 为 `853e7db3dde051850957ef8d06bcb0278cebf9af`。
- 分工勘误：组长负责 Harness 设计与编排；Dev A 负责网关、数据库、认证、Repository、适配器、检索与持久化接口；Molly 负责 AI Schema、Prompt、算子和验证器。

### 2026-08-06 / DEV-014：飞书登录、鉴权与适配器基线

- 执行人：Codex，协助 Dev A。
- 目标：按联调顺序完成登录与鉴权模块，并建立外部系统适配器边界。
- 需求依据：P0 飞书登录、Mock/Live 共用接口、安全 Cookie、当前用户与退出登录。
- 操作前状态：FastAPI 仅挂载健康检查；User 模型和 Repository 已可用；尚无适配器、鉴权依赖和 Auth Service。
- 架构决定：
  1. `feishu_mode` 独立控制飞书 Mock/Live，`ai_mode` 控制 LLM Mock/Live。
  2. 登录会话使用服务端 HMAC 签名、带过期时间的 Cookie，不在 Cookie 中保存飞书访问令牌。
  3. OAuth state 使用不可预测随机值和 HttpOnly Cookie 做回调校验。
  4. Mock 与 Live 返回相同 `FeishuUserInfo`/`FeishuDocument` 数据结构。
  5. Auth Service 通过 UserRepository 在固定比赛工作空间内 upsert 用户。
- 实际操作：
  1. 定义 `FeishuAdapter`、`LLMAdapter` Protocol 及飞书用户、文档结构。
  2. 实现 `MockFeishuAdapter` 固定用户和黄金流程 Markdown；实现 Live 飞书 OAuth 用户信息交换骨架。
  3. 实现 HMAC-SHA256 会话令牌，校验签名、UUID Claim 和过期时间。
  4. 增加数据库、用户、工作空间、适配器、SessionCodec 和 AuthService 的 FastAPI 依赖。
  5. 实现 OAuth state Cookie 校验、User 自动创建/更新和事务提交。
  6. 实现 `/auth/feishu/start`、`/auth/feishu/callback`、`/me`、`/auth/logout` 并挂载主 Router。
  7. 为 logout 接口保留 `Idempotency-Key` 契约校验；24 小时持久化幂等由后续统一中间件完成。
  8. 新增 Session、Mock 适配器、AuthService、路由、鉴权失败和 Cookie 安全属性测试。
- 文件变更：
  - `app/integrations/protocols.py`：外部适配器 Protocol 与统一数据结构。
  - `app/integrations/feishu.py`：Mock/Live 飞书适配器。
  - `app/integrations/llm.py`：Mock LLM 适配器。
  - `app/core/security.py`：签名会话 Cookie 编解码。
  - `app/core/config.py`、`.env.example`：飞书、会话和 Cookie 配置。
  - `app/api/deps.py`：认证与外部适配器依赖注入。
  - `app/services/auth.py`：飞书回调和用户 upsert 服务。
  - `app/api/v1/routes/auth.py`、`app/api/v1/router.py`：四个认证接口及挂载。
  - `app/db/repositories/entities.py`：按工作空间查询飞书用户。
  - `tests/test_auth.py`：认证模块单元与 API 测试。
  - `pyproject.toml`：将 Live 适配器使用的 `httpx` 纳入运行时依赖。
- 验证：
  - `python -m pytest -q`：21 passed；仅保留 FastAPI TestClient 上游弃用警告。
  - `ruff check app alembic main.py tests`：All checks passed。
  - `python -m compileall -q app alembic main.py tests`：通过。
  - `alembic check`：No new upgrade operations detected。
  - 本地 PostgreSQL 黄金流程：回调 302，`/me` 200，返回“演示售前顾问”。
  - 提交后清理 5 个新文件末尾的冗余空行，并以独立格式提交保留可追溯记录。
  - Windows 中文目录须使用 `.venv\\Scripts\\python.exe -m pytest`，直接调用 `pytest.exe` 会丢失项目根导入路径。
- 已知边界：
  - Live 飞书文档读取不属于本次 Auth 范围，`fetch_document` 将在 Source 模块实现。
  - 默认 `cookie_secure=true`，本地浏览器联调应使用 HTTPS；仅在明确的本地开发环境按需关闭。

### 2026-08-06 / DEV-013：配置本地 PostgreSQL 16

- 执行人：Codex，协助 Dev A。
- 目标：在无面板数据库、无管理员终端和无 Docker/WSL 的环境中提供可用 PostgreSQL。
- 操作前状态：无 5432 监听、无 PostgreSQL 服务、无 Docker 和 WSL 发行版；Chocolatey 因非管理员权限安装失败且未产生实例。
- 实际方案：用户手动安装 PostgreSQL 16 至 `D:\Software\PosterSQL\16`，系统服务 `postgresql-x64-16` 设置为 Auto 并运行。
- 实际操作：
  1. 验证 `psql`、`initdb`、`pg_ctl`、`createdb` 完整。
  2. 验证 `127.0.0.1:5432` 接受连接且使用 SCRAM-SHA-256。
  3. 创建登录角色 `taodaobao` 和同名数据库，数据库所有者为该角色。
  4. 创建被 Git 忽略的 `.env`，供 FastAPI 和 Alembic 使用。
  5. 执行真实连接和迁移验证。
- 验证：
  - PostgreSQL 服务：Running，StartMode=Auto。
  - 连接数据库：`taodaobao`。
  - 服务端版本：16.14。
  - Alembic head：`554fd08dde0a`。
  - 九张业务表创建成功。
- 安全：仅本机 127.0.0.1 使用；当前弱密码只允许本地开发，部署时必须替换。
- 遗留：早期免安装方案留下 `.local` 中约 106 MB 未完成 ZIP 和临时密码文件；清理命令被执行策略阻止。目录已被 Git 忽略，需要用户之后手动删除 `D:\神州\.local`。

### 2026-08-06 / DEV-012：数据库基础设施、迁移与 Repository

- 执行人：Codex，协助 Dev A。
- 目标：在 Service 层开发前打通异步数据库依赖、Alembic 迁移和软删除 Repository 基线。
- 需求依据：SQLAlchemy 2.0 asyncio、FastAPI `get_db()`、Alembic async 模板、首迁移和九实体 DAO。
- 操作前状态：九类模型与数据库工厂已存在，但尚无 FastAPI `get_db()`、Alembic 和 Repository。
- 计划步骤：
  1. 检查数据库 URL、本地 PostgreSQL 和 Docker 可用性。
  2. 完善 Engine/Session 生命周期及 `get_db()`。
  3. 初始化 Alembic async 环境并绑定 `Base.metadata`。
  4. 尝试执行 `alembic revision --autogenerate -m "init core models"`。
  5. 实现 BaseRepository 和九类实体 Repository。
  6. 测试软删除过滤、模型映射、迁移脚本和依赖一致性。
- 安全边界：不提交真实数据库密码；不使用 SQLite 生成错误的 PostgreSQL JSONB 首迁移。
- 实际操作：
  1. 完善异步 Engine、Session Factory、`get_db()`、事务上下文和关闭逻辑。
  2. 增加 Alembic 1.19 依赖并初始化 async 模板。
  3. 配置 `Base.metadata`、asyncpg URL、类型/default 比较和已知非原生 Enum CHECK 匹配规则。
  4. 尝试 autogenerate；首次因无数据库 URL 按预期失败并输出明确配置提示。
  5. 创建自包含首迁移 `554fd08dde0a_init_core_models.py`，含九表、索引、约束、延后外键和 downgrade。
  6. 实现 BaseRepository 的 create/get/list/count/update/soft_delete，并强制工作空间和软删除过滤。
  7. 创建九个实体 Repository。
  8. PostgreSQL 可用后执行真实 upgrade、漂移检查和 downgrade/upgrade 往返。
- 文件变更：
  - `app/db/database.py`
  - `app/db/repositories/base.py`
  - `app/db/repositories/entities.py`
  - `app/db/repositories/__init__.py`
  - `alembic.ini`、`alembic/env.py`、`alembic/versions/554fd08dde0a_init_core_models.py`
  - `tests/test_database.py`、`tests/test_repositories.py`
  - `pyproject.toml`、`app/db/models.py`
- 验证：
  - `get_db()` 正常 yield，路由异常时 rollback。
  - 九个 Repository 模型绑定正确。
  - get/list 自动包含 `workspace_id` 和 `is_deleted IS false`。
  - Alembic `current`：`554fd08dde0a (head)`。
  - Alembic `check`：No new upgrade operations detected。
  - 空库 downgrade base -> upgrade head：通过。
  - `pytest -q`：15 passed，仅有一条第三方 TestClient 弃用警告。
  - Ruff：All checks passed。
- 依赖冲突：无；新增 Alembic 1.19.0、Mako 1.4.1，与 SQLAlchemy 2.0.51/asyncpg 0.31.0 正常协作。
- 下一步：Service 层可直接注入 `get_db()` 并使用实体 Repository。

### 2026-08-06 / DEV-011：更新 Molly 协作说明

- 执行人：Codex，协助 Dev A。
- 目标：使 Molly 的操作说明与远程分支和文档清理后的实际状态一致。
- 操作前状态：main、dev、feat/gateway、feat/ai-engine 均已同步到 `4d5b860`；旧说明仍要求先合并基线、创建 AI 分支并提交内部开发文档。
- 计划修改：
  1. 改为直接切换远程现有 `feat/ai-engine`。
  2. 删除已完成的基线 PR 前置步骤。
  3. 删除提交 `docs/开发文档.md` 的要求。
  4. 将操作记录改为本地记录、飞书同步和完整 PR 描述。
  5. 明确内部需求和分工文件不得重新上传。
- 文件范围：本地 `docs/Molly_GitHub协作说明.md` 与本地 `docs/开发文档.md`，均被 `.gitignore` 忽略。
- 实际修改：
  1. 将旧基线前置步骤替换为当前四分支均位于 `4d5b860` 的事实。
  2. 首次操作改为跟踪已存在的 `origin/feat/ai-engine`。
  3. 提交范围改为 `app/ai` 和 `tests`，不再包含内部开发文档。
  4. 共享边界只保留 `app/contracts/ai.py`、`openapi.yaml`、`pyproject.toml`。
  5. 开发记录改为个人本地/飞书记录和完整 PR 描述。
  6. 增加禁止重新上传的内部文件清单。
- 验证：搜索确认不存在“创建 feat/ai-engine”“旧 dev 前置合并”或“PR 同步开发文档”的旧操作要求。
- 交付方式：该说明保持本地忽略，可通过飞书直接发送给 Molly，不上传 GitHub。

### 2026-08-06 / DEV-010：清理远程文档并建立 Molly 代码分支

- 执行人：Codex，协助 Dev A。
- 目标：GitHub 当前分支只保留协作所需代码和机器契约，不再展示详细需求、分工及内部开发说明。
- 需求依据：Dev A 要求删除 GitHub 上的详细说明、需求和分工文件，并创建 Molly 使用的代码分支。
- 操作前状态：远程 feat/gateway 包含完整代码、原始需求、分工、AI Harness 需求、开发文档和 Molly 协作说明；main/dev 仍停留在初始资料提交。
- 计划步骤：
  1. 本地保留材料，从 Git 索引取消跟踪。
  2. 使用精确 `.gitignore` 规则防止材料被重新加入。
  3. 删除 README 中指向内部开发文档的链接。
  4. 验证关键代码、OpenAPI 和测试仍完整。
  5. 提交并推送 feat/gateway。
  6. 将 main/dev 快进到清理后的稳定基线。
  7. 从最新 dev 创建并推送 feat/ai-engine。
- 清理文件：`需求详细信息.txt`、`抽象.txt`、`分工.md`、`分工.pdf`、AI Harness 详细需求、`docs/开发文档.md`、`docs/Molly_GitHub协作说明.md`。
- 保留文件：`app/`、`tests/`、`openapi.yaml`、`pyproject.toml`、`.env.example`、`.gitignore`、`README.md`、`main.py`。
- 安全边界：不删除本地文档，不重写 Git 历史，不处理来源未知的 `说明.md`。
- 实际操作：
  1. 将 7 份内部材料加入精确 `.gitignore` 规则。
  2. 使用 `git rm --cached` 取消跟踪，本地文件未删除。
  3. 删除 README 中指向内部开发文档的链接。
  4. 保留来源未知的 `说明.md`，不跟踪、不修改。
- 验证：
  - 7 份本地材料逐项 `Test-Path=True`。
  - `pytest -q`：7 passed，仅有一条第三方 TestClient 弃用警告。
  - Ruff：All checks passed。
  - OpenAPI 3.1.0：解析成功，26 个路径。
  - `git diff --check`：通过，仅有 Windows LF/CRLF 提示。
- Git 操作结果：
  - 清理提交：`4d5b860 chore(repo): remove internal planning documents`。
  - `feat/gateway` 已推送至 `4d5b860`。
  - `main` 与 `dev` 已快进并推送至 `4d5b860`。
  - 远程 `feat/ai-engine` 已从最新 dev 创建并推送。
- 未完成：Git 历史中仍保留旧提交内容；未执行风险较高的历史重写。

### 2026-08-06 / DEV-009：Molly GitHub 协作说明

- 执行人：Codex，协助 Dev A。
- 目标：建立 Molly 可直接执行的分支、同步、提交、PR 和冲突处理流程。
- 需求依据：两人并行开发，Dev A 负责网关与数据层，Molly 负责 AI 算子。
- 操作前状态：`feat/gateway` 已包含契约、脚手架和数据库模型；远程 `dev` 仍停留在初始提交，Molly 尚不能从 `dev` 获得最新基线。
- 计划步骤：
  1. 明确仓库邀请和首次克隆步骤。
  2. 明确必须先将 `feat/gateway` PR 合入 `dev`。
  3. 明确 Molly 从最新 `dev` 创建 `feat/ai-engine`。
  4. 明确目录所有权、共享文件修改规则和每日同步流程。
  5. 明确 Conventional Commits、测试、PR 和冲突处理要求。
- 实际操作：
  1. 明确 main、dev、feat/gateway、feat/ai-engine 四类分支职责。
  2. 核对远程分支，确认 dev 为 `e1699b4`、feat/gateway 为 `cd7990d`，要求先通过 PR 同步基线。
  3. 编写 Molly 首次克隆、创建分支和验证基线的命令。
  4. 固化 AI 目录所有权及共享契约修改规则。
  5. 编写每日同步、Conventional Commits、测试、PR、合并后同步和冲突处理流程。
  6. 补充开发文档与密钥安全要求。
- 文件变更：
  - `docs/Molly_GitHub协作说明.md`：新增可直接执行的协作手册。
  - `docs/开发文档.md`：登记 DEV-009。
- 验证：
  - 协作说明包含 15 个主要章节。
  - `git diff --check`：通过，仅有 Windows LF/CRLF 提示。
  - 远程分支核对：dev 尚未包含当前网关基线，说明中的前置步骤有效。
- 未完成与风险：Dev A 尚未创建 `feat/gateway -> dev` PR；Molly 必须在该 PR 合并后从最新 dev 建分支。
- 下一步：提交并推送本文档；Dev A 在 GitHub 创建基线 PR。

### 2026-08-06 / DEV-008：SQLAlchemy 2.0 九类核心模型

- 执行人：Codex，协助 Dev A。
- 目标：根据已冻结 OpenAPI 建立 PostgreSQL 数据模型与异步数据库基础配置。
- 需求依据：九类核心对象、UUIDv4 主键、JSONB 动态字段、统一时间戳和软删除、合理关系映射。
- 操作前状态：契约与 FastAPI 基线已通过提交 `a844a81` 固化，工作区干净。
- 计划步骤：
  1. 增加 SQLAlchemy、asyncpg 依赖。
  2. 创建 `DeclarativeBase`、异步 Engine 与 Session Factory。
  3. 创建 UUID、时间戳和软删除公共 Mixin。
  4. 实现 User、Source、CustomerProfile、Experience、Capability、Session、Message、SolutionRun、Job。
  5. 配置外键、唯一约束、索引和 relationship。
  6. 编写模型映射与约束测试。
  7. 执行 pytest、Ruff、Python 编译和 SQLAlchemy mapper 配置检查。
- 实际操作：
  1. 增加 SQLAlchemy asyncio 与 asyncpg 依赖并更新项目虚拟环境。
  2. 创建异步 Engine、Session Factory 和自动提交/回滚事务上下文。
  3. 创建 UUIDv4、时间戳、工作空间和软删除公共 Mixin。
  4. 创建九类核心模型、状态枚举、索引、唯一约束和关系。
  5. 使用 `Source.customer_profile_id` 建立画像和资料的一对多关系，API 的 `source_ids` 由关系派生，不增加冗余关联表。
  6. 为 Message 与 SolutionRun 的双向引用显式指定 `foreign_keys`；对响应消息外键使用命名 `use_alter` 约束消除 PostgreSQL 建表顺序环。
  7. 创建 UUID、公共审计列、JSONB、关系映射、表数量、DDL 编译和数据库驱动测试。
- 文件变更：
  - `pyproject.toml`：增加 `sqlalchemy[asyncio]` 与 `asyncpg`。
  - `app/db/database.py`：异步数据库基础配置。
  - `app/db/models.py`：九类 SQLAlchemy 2.0 模型。
  - `app/db/__init__.py`：导出 Base 和九类模型。
  - `tests/test_models.py`：模型结构和 PostgreSQL DDL 测试。
  - `docs/开发文档.md`：更新目录、状态和本记录。
- 验证：
  - `pytest -q`：7 passed，仅保留一条第三方 TestClient 弃用警告。
  - Ruff：All checks passed。
  - Python 编译：通过。
  - SQLAlchemy `configure_mappers()`：通过，无关系歧义。
  - PostgreSQL DDL 编译：9 张表全部成功，无循环排序警告。
- 架构决定：
  - 所有业务主键和外键使用 `uuid.UUID`/PostgreSQL UUID。
  - CustomerProfile.profile、Experience.data、Capability.data 及 SolutionRun 快照/结果使用 JSONB。
  - Job.target_id 是 Source/Profile 多态目标，不绑定错误的单表外键。
  - 所有表包含 `created_at`、`updated_at`、`is_deleted`；所有业务表包含 `workspace_id` 用于隔离。
- 未完成与风险：尚未创建 Alembic 环境和首版迁移，也未连接真实 PostgreSQL 执行迁移。
- 下一步：建立 Alembic 迁移基线，并在 PostgreSQL 测试库执行 upgrade/downgrade 验证。

### 2026-08-06 / DEV-007：冻结 OpenAPI V0.5 契约

- 执行人：Codex，协助 Dev A。
- 目标：在数据库建模前修复分页、实体关联、任务状态和业务 ID 类型约束。
- 需求依据：架构交叉评审提出的六类 OpenAPI 修正项。
- 操作前状态：OpenAPI 可解析且本地引用完整，但 ListResponse 使用 `allOf`，部分关系字段缺失，Job/Run 状态和业务 ID 约束尚未统一。
- 计划步骤：
  1. 拍平所有列表响应并增加 `has_more`。
  2. 补齐 Source、CustomerProfile、UpdateProfileRequest 和 Message 的关联字段。
  3. 分离 Job 与 SolutionRun 状态及职责。
  4. 全量增加业务 UUID 格式约束。
  5. 清理错误码、审核动作和健康检查字段。
  6. 运行 YAML、引用、分页、写接口幂等和 UUID 自动审计。
  7. 将当前基线提交至 `feat/gateway`。
- 实际操作：
  1. 将 5 个 ListResponse 拍平为单层 `data.items/page/page_size/total/has_more`，删除分页继承 Schema。
  2. 为 Source 增加 `customer_profile_id`，为 CustomerProfile 增加 `is_deleted`，为 UpdateProfileRequest 增加 `source_ids`。
  3. 从 Job 类型中删除 `solution_generation`，将 Job 和 Run 状态统一为四态。
  4. 新增 `BusinessId` UUID Schema，并覆盖路径参数、请求、响应、数组外键和引用字段。
  5. 删除两个无资产错误码；增加 `reopen`、`solution_run_id` 和 `ai_mode`。
  6. 为业务实体补齐契约中的审计时间戳和软删除字段，作为数据库模型基线。
  7. 同步健康接口、Python 错误枚举、测试和 AI Harness 需求文档。
- 文件变更：
  - `openapi.yaml`：结构性修正并冻结 P0 数据契约。
  - `app/api/v1/routes/health.py`：响应增加 `ai_mode`。
  - `app/core/errors.py`：删除两个不再使用的错误码。
  - `tests/test_health.py`：验证 `ai_mode`。
  - `淘到宝引擎V0.5_后端与AI_Harness需求文档.md`：同步错误语义。
  - `docs/开发文档.md`：记录本次操作。
- 验证：
  - YAML 解析：通过，26 个路径。
  - 5 个 ListResponse：无 `allOf`、无嵌套 `data`，均含 `has_more`。
  - UUID 自动审计：0 个问题。
  - 本地 `$ref`：248 个，0 个缺失。
  - 16 个 POST/PATCH：0 个缺失幂等 Header。
  - `pytest -q`：1 passed，保留一条第三方弃用警告。
  - Ruff：All checks passed。
  - `git diff --check`：通过，仅有 Windows LF/CRLF 提示。
- 架构决定：无经验或能力匹配是正常空快照，不再使用专门错误码；方案生成仅由 SolutionRun 轮询。
- 未完成与风险：契约字段已冻结，后续数据库模型必须以该版本为准。
- 下一步：提交基线后登记 DEV-008，创建 SQLAlchemy 2.0 数据模型。

### 2026-08-06 / DEV-001：项目资料阅读与 MVP 基线梳理

- 执行人：Codex，协助 Dev A。
- 目标：理解项目、MVP 范围和个人分工。
- 需求依据：`需求详细信息.txt`、`抽象.txt`、`分工.md`、`分工.pdf`。
- 操作前状态：目录中只有需求和分工文档，没有代码和 Git 仓库。
- 操作步骤：
  1. 盘点目录文件。
  2. 阅读需求、能力抽象和分工文档。
  3. 对照正式需求与分工稿，识别技术范围偏差。
- 结论：
  - V0.5 使用模块化单体和 PostgreSQL。
  - 不引入向量数据库、Redis 强依赖、Multi-Agent 和复杂基础设施。
  - Dev A 负责网关、数据、飞书、任务和系统编排；Molly 负责 AI 算子。
- 验证：确认 PDF 与 Markdown 分工内容一致。
- 风险：分工稿原先提出 ChromaDB/pgvector 和 6 张主表，与正式需求不一致，后续按正式需求修正。

### 2026-08-06 / DEV-002：Git 仓库初始化与协作分支建立

- 执行人：Codex，协助 Dev A。
- 目标：建立可协作、可追溯的版本管理基线。
- 操作前状态：`D:\神州` 不是 Git 仓库。
- 操作步骤：
  1. 创建 `README.md` 和 `.gitignore`。
  2. 初始化 Git，设置当前仓库提交身份。
  3. 建立首次提交 `e1699b4 docs: initialize project requirements`。
  4. 关联 `https://github.com/wxh042/xianjintuan-engine.git`。
  5. 创建 `main`、`dev`、`feat/gateway`。
  6. 使用 Git Credential Manager 设备码完成 GitHub 登录。
  7. 为当前仓库配置 GitHub 代理 `http://127.0.0.1:7897`。
  8. 推送三个分支并建立上游跟踪关系。
- 文件变更：`README.md`、`.gitignore` 及初始需求文档进入首次提交。
- 验证：`git ls-remote --heads origin` 返回三个远程分支，均指向 `e1699b4`。
- 架构或协作决定：功能开发在个人分支完成，先合入 `dev` 联调，通过后再进入 `main`。
- 风险：GitHub 访问依赖本机 `127.0.0.1:7897` 代理运行。

### 2026-08-06 / DEV-003：后端与 AI Harness 需求文档

- 执行人：Codex，协助 Dev A。
- 目标：把产品需求转化为可执行的后端和 AI 协作契约。
- 操作步骤：
  1. 编写后端职责、P0/P1 范围和九类数据对象。
  2. 定义 AI Harness 运行边界。
  3. 列出 P0 HTTP 接口、错误码、验收标准和开发顺序。
  4. 根据架构评审修正检索设计。
- 文件变更：
  - `淘到宝引擎V0.5_后端与AI_Harness需求文档.md`：新增并修正需求基线。
- 关键决定：
  - 增加 `extract_search_intent(context) -> SearchIntent`。
  - Dev A 负责 DB 检索，AI 不接收全量资产。
  - `generate_solution` 只接收固化的 Top-K `RetrievalSnapshot`。
  - Molly 抛出 LLM 异常，Dev A Harness 统一捕获和落库。
  - 资产软删除不影响历史 `SolutionRun` 快照展示。
- 验证：搜索确认文档不存在旧 `retrieve(experiences: list[dict])` 契约。

### 2026-08-06 / DEV-004：P0 OpenAPI 契约初稿

- 执行人：Codex，协助 Dev A。
- 目标：建立机器可读的前后端唯一 HTTP 契约。
- 操作步骤：
  1. 建立 OpenAPI 3.1 文档。
  2. 定义 P0 路由、请求、响应、分页、枚举和错误码。
  3. 为全部 POST/PATCH 引用统一 `Idempotency-Key` 参数。
  4. 定义八区块方案、证据边界和独立检索快照。
  5. 执行 YAML、幂等覆盖和 `$ref` 完整性检查。
- 文件变更：
  - `openapi.yaml`：新增 P0 API 契约。
- 验证：
  - YAML 解析成功，版本为 3.1.0。
  - 26 个路径。
  - 16 个 POST/PATCH，缺失幂等 Header 数量为 0。
  - 209 个本地 `$ref`，缺失数量为 0。
- 未完成与风险：OpenAPI 是目标契约，目前并非所有路由都已经实现。

### 2026-08-06 / DEV-005：FastAPI 基础脚手架

- 执行人：Codex，协助 Dev A。
- 目标：提供可启动、可测试、可扩展的服务端基线。
- 操作步骤：
  1. 创建 `pyproject.toml` 和 `.env.example`。
  2. 创建 FastAPI 应用工厂和根入口。
  3. 创建 V1 路由聚合器和健康检查示例。
  4. 创建配置、Request ID、响应、错误码和全局异常处理模块。
  5. 创建幂等 Header 校验依赖。
  6. 创建 AI Protocol 临时契约。
  7. 创建健康检查测试。
  8. 创建 `.venv` 并安装开发依赖。
  9. 启动本地服务并调用健康接口。
- 文件变更：
  - `main.py`、`app/main.py`：应用入口和工厂。
  - `app/api/v1/router.py`、`app/api/v1/routes/health.py`：路由挂载。
  - `app/core/*`：跨模块基础能力。
  - `app/contracts/ai.py`：AI 调用边界。
  - `pyproject.toml`、`.env.example`：依赖和配置示例。
  - `tests/test_health.py`：健康检查测试。
- 验证：
  - `pytest -q`：1 passed；存在一条第三方 TestClient 弃用警告。
  - `ruff check app main.py tests`：All checks passed。
  - Python 编译：通过。
  - 应用导入：通过，实际挂载 `/api/v1/health`。
  - HTTP 健康检查：返回 `status=ok`、`database=not_configured`、`ai=mock`。
- 未完成与风险：数据库和 AI 仅为未配置或 Mock 状态。

### 2026-08-06 / DEV-006：建立强制开发文档机制

- 执行人：Codex，协助 Dev A。
- 目标：确保后续每一步开发、验证和架构变化可追溯。
- 操作前状态：开发内容分散在需求文档和对话记录中，没有统一开发日志。
- 操作步骤：
  1. 创建本文档。
  2. 补录 DEV-001 至 DEV-005 的已完成工作。
  3. 固化任务开始、执行、验证、提交前的记录规范。
  4. 记录当前实现、未完成项、风险和运行方式。
- 文件变更：
  - `docs/开发文档.md`：新增持续开发文档。
- 验证：
  - 文档包含 11 个一级章节和 DEV-001 至 DEV-006 六条操作记录。
  - `git diff --check`：通过，仅提示 Windows 下 README 的 LF/CRLF 转换，不存在空白错误。
  - `pytest -q`：1 passed；原有第三方 TestClient 弃用警告仍存在。
  - `git status`：本文档与阶段一代码均位于 `feat/gateway` 工作区，尚未提交。
- 下一步：进入数据库与九类对象设计前，先新增对应“进行中”任务记录。

## 11. 下一阶段计划

下一阶段为数据库与领域模型基线，建议顺序：

1. 冻结九类数据对象的字段、索引和关系。
2. 增加 SQLAlchemy、asyncpg 和 Alembic 依赖。
3. 创建异步数据库 Session 和 Base。
4. 创建首版迁移。
5. 优先实现 Workspace/User、Source、Job 和幂等记录。
6. 为软删除快照和工作空间隔离编写自动化测试。

开始该阶段前，必须先在第 10 节新增 DEV-007 进行中记录。
