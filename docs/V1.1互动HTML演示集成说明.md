# 淘到宝引擎 V1.1 互动 HTML 演示集成说明

## 1. 产品位置

互动 HTML 不是独立工具，也不接收任意自由文本。它是“演示稿”工作台中的第二种产物形态：

```text
快速方案 / Deep Research
→ 后端 Trust Gate
→ released Claim + granted Evidence
→ PresentationInputSnapshot
→ AI 互动 HTML 编排
→ 事实绑定审计 + 静态安全审计
→ HtmlArtifact
→ 业务端沙箱预览与 HTML 导出
```

标准可信 HTML/PPTX Renderer 保持不变。互动模式用于比赛演示和客户现场讲解，强调章节导航、滚动
叙事、悬停和点击展开；当前不承诺转换为 PPTX/PDF。

## 2. 增量接口

`POST /api/v1/solution-runs/{run_id}/interactive-presentations`

请求和响应定义在 `docs/openapi-presentation-v1.1-incremental.yaml`。既有冻结 `openapi.yaml` 与
`app/contracts/ai.py` 五个方法签名不变；互动生成通过演示 Provider 适配层实现，不增加第六个
`AIEngine` 方法。

## 3. 可信与安全边界

- 只有 `RELEASE` 或 `DOWNGRADE` 方案可创建任务。
- 每条 released Claim 在 HTML 中必须恰好出现一次，文本逐字一致。
- Evidence 如展示，ID 与 quote 必须来自同一输入快照并逐字一致。
- 未绑定可见文字只允许固定系统标签，模型不能自行补充客户结论或量化效果。
- 禁止外部资源、网络请求、表单、浏览器存储、Cookie、父页面访问、跳转和动态文字注入。
- 浏览器以 `sandbox="allow-scripts"` 运行产物，不授予 `allow-same-origin`。
- 任何事实或安全违规都会使 Worker 失败，不写入可用 `HtmlArtifact`。

## 4. 运行配置

```env
APP_INTERACTIVE_HTML_MODE=live
APP_INTERACTIVE_HTML_BASE_URL=https://api.deepseek.com
APP_INTERACTIVE_HTML_API_KEY=仅写部署环境
APP_INTERACTIVE_HTML_MODEL=deepseek-chat
APP_INTERACTIVE_HTML_TIMEOUT_SECONDS=180
```

`mock` 模式只生成可验证的结构样稿并明确停在 `needs_review`；`live` 模式调用模型，产物通过事实与
安全审计后进入 `ready`。密钥不得进入 Git、浏览器或接口响应。

## 5. 当前限制

- 静态审计通过不等于浏览器视觉质量测量；`render_report.visual_check` 会明确记录检查范围。
- 互动稿的业务文字被锁定；需要改事实时应返回方案/Trust Gate 重新生成，而不是在演示层修改。
- 当前只支持 HTML 导出；PDF/PPTX 继续使用标准 Renderer。

## 6. 2026-08-14 验证结果

- 互动模块、Provider 与主前端定向测试：15 passed。
- 真实 `deepseek-chat`：约 40 秒生成 18.9KB 单文件 HTML，Claim 逐字绑定与安全审计均通过，状态
  `ready`。
- Chrome：1440×900 与 390×844 均无横向溢出；sticky 导航、点击展开可用，控制台无错误。
- 全量回归：387 passed、16 skipped；另有 3 项因当前本机未配置 PostgreSQL 而失败，错误均为
  `Database is not configured` / 健康状态 `not_configured`，不属于互动模块回归失败。
- Ruff、Python compileall、静态前端 JavaScript 解析及增量 OpenAPI YAML 解析通过。
- 冻结 `openapi.yaml` 与 `app/contracts/ai.py` 未修改，哈希仍分别为
  `bb1fa292c5b55a948fae315cb569fc2611858659`、
  `5fcef55c9ec601c1e21f404a324c0d0e1e4dd771`。
