# `final` 分支完整交付清单

交付日期：2026-08-17

## 交付范围

- 后端、AI 服务、飞书联动、可信门禁、情报与招标、方案演练、Deep Research、专家协作、互动 HTML、邀请码与演示入口的当前完整实现。
- Alembic 数据库迁移、Docker Compose 部署配置、联调脚本、录制准备脚本和自动化测试。
- V0.5、V1、V2 的产品需求、模块 PRD、依赖图、任务池、交互原型、复赛方案和测试报告。
- HTTP 主契约、V1/V1.1 增量契约，以及后端与 AI 的五方法冻结契约。

## 契约边界

- 主 HTTP 契约：仓库根目录 `openapi.yaml`。
- AI 冻结契约：`app/contracts/ai.py`。
- 本次交付未修改上述两个冻结文件。
- 演示交付能力通过增量契约记录，没有覆盖 V0.5 quick 流程。

## 发布前验证

- Pytest：`469 passed, 19 skipped, 1 warning`。
- Ruff：通过。
- Python `compileall`：通过。
- `git diff --check`：通过。
- 暂存内容敏感信息扫描：未发现 API Key、私钥或私有 `.env` 文件。

19 项跳过测试依赖显式启用的外部或数据库联调环境，不使用 Mock 冒充真实能力；相关真实环境需按对应测试文件和部署文档配置后单独执行。

## 未纳入 Git 的内容

- `.env`、云端运行密钥、飞书 App Secret、模型 API Key 和 SSH 私钥。
- `.local/`、测试截图、缓存、日志、数据库运行数据和构建产物。
- 服务器上的运行时备份与历史 Docker 镜像。

这些内容不属于源代码交付物，应通过环境变量、密钥管理或部署流程单独配置。
