# 淘到宝引擎 V1 云端集成验收报告

验收日期：2026-08-09  
基线分支：`feat/v1-integration-acceptance`  
基线提交：`78a753c`
云端镜像：`taodaobao-app:v1-invite-replay-78a753c`

## 结论

V1 当前代码与云端基础链路通过本轮集成回归；真实飞书 OAuth 和用户权限下的资料枚举已通过。系统仍使用 AI Mock，因此不能据此判定真实 AI 效果或完整飞书专家协作通过。机器人建群、专家真实回复、事件回流、HTTPS 与独立长任务 Worker 仍是最终验收前置项。

## 自动回归

| 项目 | 结果 |
|---|---|
| 全量 pytest（含隔离 PostgreSQL/pgvector） | 170/170 通过 |
| Deep Research/专家协作数据库流程 | 通过，未跳过 |
| Ruff | 通过 |
| 前端脚本语法 | 通过 |
| 冻结 `openapi.yaml` | 未修改 |
| `app/contracts/ai.py` 五方法签名 | 未修改 |

Windows 运行 pytest 时 asyncpg 在部分线程退出阶段打印 `access violation`，但测试进程完成并返回 170/170 通过。该提示未在 Linux 云端服务中复现。

## 本轮修复

- 飞书资料选择器从静态演示项改为调用 `/api/v1/feishu/resources`，支持文档、妙记、分页和多选。
- 飞书链接、妙记与粘贴文本导入按用户选择传递 `customer_profile`、`experience` 或 `capability`，不再统一写死经验提取。
- 页面明确展示 AI/飞书真实或模拟模式，并标出演示资料。
- 删除前端写死邀请码；邀请码由服务端校验。
- 邀请验证 Cookie 改为签名、带随机数、十分钟有效、HttpOnly、SameSite Strict，并在 OAuth 启动时由服务端原子消费；重复提交原始凭证也会被拒绝。
- 增加服务器邀请码启用、轮换、检查和关闭脚本；邀请码只保存在权限 0600 的服务器文件和运行环境中。
- 页面启动时重新读取服务状态，避免 localStorage 显示过期运行模式。
- 合并合作伙伴的非 root 多阶段 Docker、Caddy 和生产 Compose 配置，并强制 `start.sh` 使用 LF，避免 Windows 构建后的 Linux 入口失败。

## 云端验收

| 项目 | 结果 |
|---|---|
| 服务健康 | `status=ok` |
| PostgreSQL | `database=ok` |
| 飞书模式 | `live/configured` |
| AI 模式 | `mock`，页面明确标记 |
| 真实飞书 OAuth | 通过 |
| 真实飞书文档枚举 | 通过，当前授权账号返回 3 项 |
| 真实飞书妙记枚举 | 通过，当前授权账号返回 0 项 |
| 邀请码未验证拦截 | 401 `INVITE_CODE_INVALID` |
| 正确邀请码验证 | 200 |
| OAuth 启动 | 200 |
| 同一邀请凭证重复使用 | 401，符合单次使用规则 |
| 应用容器内存 | 约 68 MiB / 900 MiB 限额 |
| PostgreSQL 容器内存 | 约 41 MiB / 700 MiB 限额 |

云端保留了更新前容器和源码副本用于回滚。运行环境和邀请码文件权限均为 0600，验收输出未显示任何密钥、Token 或邀请码值。

当前邀请码消费账本与现有单进程 Uvicorn 部署匹配；未来改为多 Worker 或多实例前，应迁移到 PostgreSQL 或 Redis 共享账本。

## 追加功能验收（2026-08-09）

| 项目 | 结果 |
|---|---|
| 飞书资料同步到资料库 | 通过；真实文档已出现在资料库 |
| 已确认客户画像复用 | 通过；快速方案与 Deep Research 均读取 A客户画像 |
| 快速方案首轮生成 | 流程通过；AI Mock 输出与命中资产存在一致性缺陷 |
| 快速方案多轮追问 | 通过；同一会话保留首轮与追问内容 |
| Deep Research 五阶段 | 通过；进度到 85%，状态进入 `waiting_expert` |
| 研究证据快照 | 命中经验 1 条、能力 1 项、知识缺口 1 项 |
| 专家协作草稿 | 通过；同一研究上下文生成 1 条待员工确认协作 |
| 真实专家推荐 | 未通过；当前证据没有真实作者、贡献者或审核人 |
| 飞书基础建群与机器人消息 | 通过；真实创建 `[测试] 淘到宝建群联调`，成员仅机器人和当前发起人，并成功发送验收说明 |
| 正式专家协作建群 | 未执行；没有真实候选专家，基础联调群不替代专家推荐闭环验收 |

本轮没有处理发现的缺陷。当前未关闭问题统一记录在 `docs/V1_BUG_LIST.md`。

## 尚未通过

1. 真实百炼 AI 尚未配置，快速方案和 Deep Research 当前只能做 Mock 流程验收。
2. 已通过“机器人＋当前发起人”的真实建群和消息基础联调，但尚未以真实专家候选完成正式专家协作建群。
3. 尚未完成专家真实回复 → 飞书事件回调 → `waiting_expert` 恢复 → 最终报告的在线闭环。
4. 当前公网为 HTTP IP；尚未配置域名、HTTPS、反向代理、备份、监控和独立 Worker。
5. 当前账号没有妙记条目，因此接口成功但未覆盖真实妙记正文导入。
6. 飞书资料进入资料库后无法继续沉淀到经验库或能力库，见 `BUG-V1-001`。
7. 快速方案命中统计、来源快照与正文依据不一致，见 `BUG-V1-003`。

## 验收边界

本报告可证明当前 V1 集成基线、云端运行、真实登录、真实资料列表和邀请码门禁可用；不能替代大赛前的真实 AI 效果验收、机器人外部动作确认和完整飞书专家协作验收。
