# 淘到宝引擎 AI 服务模拟数据验收与修复报告

验收日期：2026-08-08
范围：AI 服务及本地模拟数据；不包含飞书 OAuth、文档拉取、机器人建群和飞书消息回调。

## 结论

在上述范围内，AI 服务验收通过。五个冻结方法均已用模拟资料验证；本轮发现的结构漂移、状态误判、重复意图、Deep Research 问题 ID 缺失和复合引证问题均已修复并回归通过。未修改冻结方法签名或既有 HTTP 契约。

这不是“V1 全系统验收通过”：真实飞书联动、生产数据库检索和专家回复驱动后端长任务恢复仍需集成验收。

## 自动测试结果

| 测试项 | 结果 |
|---|---|
| 模拟数据完整性 | 通过：23 个文件、13 个来源、3 个客户、6 条经验、16 项能力、16 条查询 |
| 模拟数据断言 | 907/907 通过 |
| AI 单元、契约与新增回归测试 | 108/108 通过 |
| Mock 业务回归 | 24 条；Schema、引用、拒答、边界通过率均为 100% |
| Mock 资产命中 | 0%，符合 Mock 不执行真实语义检索的设计，不作为线上命中率结论 |
| Ruff | `app/ai` 与 `tests/ai` 全部通过 |

## 本轮问题与修复

| 问题 | 修复 | 回归结果 |
|---|---|---|
| 画像模型把 `industry` 输出为数组，并使用非法 `fact_sources.field=customer_name` | 对允许的事实字段和列表/标量类型做归一化；丢弃 Schema 外事实字段；Prompt 明确字段约束 | 真实 `qwen-plus` 最终 Schema 通过；仍发生 1 次自动结构修复，不影响最终结果 |
| 已完成并签署验收记录的 `EXP-001` 被标为 `concept_only` | 概念资料优先识别；有结果且包含明确验收记录时确定性归为 `historical_record` | 原始 `SRC-EXP-001` 真实模型复测输出 `historical_record` |
| 能力抽取出现列表/标量类型漂移 | 归一化前置条件、限制、依赖、标签和审核警告 | 真实模型输出 4 项能力，Schema 通过且无需结构修复 |
| 同义约束和信息缺口重复 | 增加标点/近似文本及业务语义键去重 | 真实措辞回归用例通过；图片出园、AI 自动控制、相机、样本、验收、接口、服务器和预算等同义项可合并 |
| Deep Research 规划的 6 个子问题缺少 `question_id` | Prompt 强制要求 ID；归一化层稳定补齐 `Q1...Qn` | 真实模型规划通过，6 个 ID 完整，状态为 `awaiting_expert` |
| 一个陈述可能提到多个资产但只挂一个来源 | 生成 Prompt 要求一条事实只对应一个资产；验证器拒绝跨资产复合陈述 | 真实黄金案例 40 个输出项无跨资产复合引证 |

## 五个冻结方法真实模型验收

| 方法 | 结果 | 摘要 |
|---|---|---|
| `extract_profile` | 通过 | 客户、行业、约束、缺口和事实字段最终符合 Schema；自动结构修复 1 次 |
| `extract_experience` | 通过 | `EXP-001` 来源、结果和可信状态正确，输出 `historical_record` |
| `extract_capabilities` | 通过 | 从 `SRC-PRD-001` 得到 4 项能力，数量与人工真值一致 |
| `extract_search_intent` | 通过 | 正确识别场景、硬约束和缺口，并在输出前确定性去重 |
| `generate_solution` | 通过 | 八区块、可信边界、快照来源和逐条事实审计均通过 |

## 真实黄金方案闭环

- 输出文件：`evals/latest_verified_solution_after_fix.json`。
- 独立质检：`passed=true`。
- 事实审计：33 条，失败 0 条。
- 自动重写：0 次。
- 输出项：40 个；带资产来源 18 个；跨资产复合引证 0 个。
- 来源全部来自本次固定检索快照，历史答案不会被后续资产变化改写。
- 实施顺序保持 `ai_inference`；相机、样本、MES 写入、服务器、预算和验收指标保持 `pending_confirmation`。

## Embedding

- 模型：`text-embedding-v4`，维度 1024。
- Q-001 与同义需求 Q-002 相似度：0.8793。
- Q-001 与无关会员营销需求 Q-009 相似度：0.5549。
- 三条输入指纹均不同。

## V1 独立 AI 节点

| 节点 | 结果 | 说明 |
|---|---|---|
| Deep Research `planning` | 通过 | 6 个子问题均有稳定 ID；因知识缺口进入 `awaiting_expert` 符合状态机 |
| 专家匹配与问题路由 | 通过 | 只推荐输入候选 `USER-001`，引用真实贡献 `CONTRIB-001` |
| 无候选人拒绝邀请 | 通过（离线） | 后端未提供候选时不调用模型并明确不邀请原因 |
| 两个专家协作入口内容一致 | 通过（离线） | 两个入口使用同一研究上下文 |
| 专家回复可信边界 | 通过（离线） | 回复保留任务、问题、作者和消息链接，边界为 `pending_confirmation` |

## 未覆盖与风险

1. 未测试真实飞书 OAuth、文档权限、资料拉取、机器人建群、卡片和回调。
2. 未测试专家回复进入后端后把持久化长任务从 `waiting_expert` 恢复为 `researching`。
3. 未在生产数据库验证全量 16 条查询的召回率、排序和权限过滤。
4. 画像真实模型仍需一次结构修复；目前有确定性兜底，但后续模型或 Prompt 变更应持续监控 `structure_repair_count`。
5. Mock 资产命中率为 0 是测试桩能力边界，不能替代真实检索指标。
