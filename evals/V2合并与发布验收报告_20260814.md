# 淘到宝引擎 V2 合并与发布验收报告

> 历史记录：本报告保留当时的分支验收事实，其中 Open Enrich Mock 结论已被
> 2026-08-15 的“生产运行无 Mock”规则取代，不得据此宣称当前线上公开情报已接通。

验收日期：2026-08-14  
验收分支：`v2`

## 合并范围

- 基线：`8.13_backbone`
- 情报与招标：`feat/v2-intelligence-tender`，包含提交 `b4b4853`
- 演练与展示：`feat/v2-rehearsal-presentation`（已在基线祖先链）
- AI 可信服务：`feat/ai-engine`（已在基线祖先链）
- V1.1 Renderer：`8.9_backbone` 的可信渲染提交链

## 已验证结果

- 冻结契约：`openapi.yaml`、`app/contracts/ai.py` 未改变。
- 数据库：空 PostgreSQL 可升级到 merge Head `f2a14c8e7d90`；`alembic check` 无漂移。
- Python 全量回归：`381 passed, 18 skipped`。
- Ruff、Compileall：通过。
- Open Enrich Sidecar：7 项 Node 测试通过。
- PPTX Renderer：Node 语法检查通过。
- 演示资产：34 项资产、31 项运行时可用，0 错误。
- R1 中文 HTML/PPTX：6 页生成成功；三视口无溢出、无缺失标题；对比度均高于 4.5:1；事实哈希保持一致。
- V2 前端：主业务页提供情报与招标入口；两个工作台使用真实 `/api/v1`、同源 Session Cookie、统一响应信封和写请求幂等键。

## 跳过项说明

18 项跳过来自测试环境显式条件（真实外部服务、可选渲染器或平台能力），不属于失败。真实飞书建群会产生外部人员和消息副作用，本轮自动回归不重复触发；既有 V1 Bug List 中该项保留人工验收记录。

## 发布边界

生产服务器未配置 Bright Data/OpenRouter 凭据时，Open Enrich 使用内置契约 Mock，不宣称真实公开情报采集已上线。其余 V2 数据治理、画像审批、招标响应、可信服务、演练与展示链路可独立发布验证。

## 云端发布结果

- 公网入口：`http://8.218.59.190/`
- 发布镜像：`taodaobao-app:v2-7d89e27`
- 生产迁移：`f2a14c8e7d90 (head) (mergepoint)`，唯一 Head。
- 服务状态：app、worker、PostgreSQL 均运行正常；durable worker 已启动。
- 健康接口：`status=ok`、`database=ok`、`ai=ok/live`、`feishu=configured/live`、`sidecar_status=not_configured`、`stale_leases=0`。
- 公网页面：首页、情报工作台、招标工作台均返回 HTTP 200；首页标识为 V2。
- 鉴权边界：未登录请求 `/api/v1/me` 返回 HTTP 401。
- 发布前备份：`/opt/taodaobao/backups/pre-v2-20260814T0055.sql.gz`，已通过 gzip 完整性与非空校验。
- 回滚镜像：旧版 `taodaobao-app:8.13-4a67ab7` 保留，未执行数据库降级。

## 依赖安全审计

- Open Enrich Sidecar：npm audit 0 漏洞。
- PPTX Renderer：上游 `pptxgenjs@4.0.1 -> image-size@1.2.1` 有 2 个 High 级图片解析 DoS 公告。生产 Renderer 不接收任意外部图片，当前未暴露对应入口；暂无可用的不受影响上游版本，已登记 `EXT-V2-002`，不采用破坏 API 的降级方案。

## 缺陷

本次发现的合并和回归问题已记录在 `docs/V2_BUG_LIST.md`。代码类缺陷均已关闭；仅真实 Open Enrich 供应商凭据属于外部依赖。
